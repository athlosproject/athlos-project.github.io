---
title: "Chronic pulmonary disorders"
output:
  html_document:
    highlight: tango
    number_sections: yes
    theme: united
    toc: yes
    toc_depth: 6
    toc_float:
      collapsed: yes
      smooth_scroll: yes
---

<!-- ########################################################## --> 
<!-- # Installation, loading packages -->
```{r setup, include=FALSE}
source("../../setup.r")
```
<!-- ########################################################## --> 
<!-- # Loading DS description -->
```{r main, child='../../_DS_Rmd/cpdDS.Rmd'}
```
```{r global, echo=F}
source('../../_DS_Rmd/cpdDS.R')
```



# Data process




## Cuba


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-Cuba-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** |`NA` |
| **Description** |  |

```{r assign cuba_w1, echo=F}

opal.assign.table.tibble(o, 't1066_cuba_w1','10-66.1066-Cuba-Baseline',variables=list('COAD'), missings = T)
t1066_cuba_w1 <- opal.execute(o,'t1066_cuba_w1')

```

```{r local cuba_w1, echo=F}

vbl <- t1066_cuba_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_cuba_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_cuba_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo cuba_w1}

t1066_ds_cuba_w1 <- tibble(id=t1066_cuba_w1$id)
t1066_ds_cuba_w1$h_cpd <- car::recode(t1066_cuba_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript cuba_w1, echo=F}

vbl <- t1066_ds_cuba_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_cuba_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation cuba_w1, echo=F}

AfterH <- table(t1066_ds_cuba_w1$h_cpd)
BeforeH <- table(t1066_cuba_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1","NA->999")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-Cuba-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** |`NA` |
| **Description** |  |

```{r assign cuba_w2, echo=F}

opal.assign.table.tibble(o, 't1066_cuba_w2','10-66.1066-Cuba-Baseline',variables=list('COAD'), missings = T)
t1066_cuba_w2 <- opal.execute(o,'t1066_cuba_w2')
opal.assign.table.tibble(o, 't1066_cuba_w2_id','10-66.1066-Cuba-Followup',variables=list('HOUSEID'), missings = T)
t1066_cuba_w2_id <- opal.execute(o,'t1066_cuba_w2_id')

```

```{r local cuba_w2, echo=F}

t1066_cuba_w2 <- left_join(t1066_cuba_w2_id, t1066_cuba_w2, by = "id")[,c(1,3)]

vbl <- t1066_cuba_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_cuba_w2)[3])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_cuba_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo cuba_w2}

t1066_ds_cuba_w2 <- tibble(id=t1066_cuba_w2$id)
t1066_ds_cuba_w2$h_cpd <- car::recode(t1066_cuba_w2$COAD, "NA=999; 0=999")

```


#### Statistics of the new harmonized variable

```{r descript cuba_w2, echo=F}

vbl <- t1066_ds_cuba_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_cuba_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation cuba_w2, echo=F}

AfterH <- table(t1066_ds_cuba_w2$h_cpd)
X <- table(t1066_cuba_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)]))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## India


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  |  `1066-India-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign india_w1, echo=F}

opal.assign.table.tibble(o, 't1066_india_w1','10-66.1066-India-Baseline',variables=list('COAD'), missings = T)
t1066_india_w1 <- opal.execute(o,'t1066_india_w1')

```

```{r local india_w1, echo=F}

vbl <- t1066_india_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_india_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_india_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo india_w1}

t1066_ds_india_w1 <- tibble(id=t1066_india_w1$id)
t1066_ds_india_w1$h_cpd <- car::recode(t1066_india_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript india_w1, echo=F}

vbl <- t1066_ds_india_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_india_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation india_w1, echo=F}

AfterH <- table(t1066_ds_india_w1$h_cpd)
BeforeH <- table(t1066_india_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1","NA->999")
kable(C)

```





## Urban China


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-UrbanChina-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** | |

```{r assign UCh_w1, echo=F}

opal.assign.table.tibble(o, 't1066_UCh_w1','10-66.1066-UrbanChina-Baseline',variables=list('COAD'), missings = T)
t1066_UCh_w1 <- opal.execute(o,'t1066_UCh_w1')

```

```{r local UCh_w1, echo=F}

vbl <- t1066_UCh_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_UCh_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_UCh_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo UCh_w1}

t1066_ds_UCh_w1 <- tibble(id=t1066_UCh_w1$id)
t1066_ds_UCh_w1$h_cpd <- car::recode(t1066_UCh_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript UCh_w1, echo=F}

vbl <- t1066_ds_UCh_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_UCh_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation UCh_w1, echo=F}

AfterH <- table(t1066_ds_UCh_w1$h_cpd)
BeforeH <- table(t1066_UCh_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-UrbanChina-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** | |

```{r assign UCh_w2, echo=F}

opal.assign.table.tibble(o, 't1066_UCh_w2','10-66.1066-UrbanChina-Baseline',variables=list('COAD'), missings = T)
t1066_UCh_w2 <- opal.execute(o,'t1066_UCh_w2')
opal.assign.table.tibble(o, 't1066_UCh_w2_id','10-66.1066-UrbanChina-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_UCh_w2_id <- opal.execute(o,'t1066_UCh_w2_id')

```

```{r local UCh_w2, echo=F}

t1066_UCh_w2 <- left_join(t1066_UCh_w2_id, t1066_UCh_w2, by = "id")[,c(1,3)]

vbl <- t1066_UCh_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_UCh_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_UCh_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo UCh_w2}

t1066_ds_UCh_w2 <- tibble(id=t1066_UCh_w2$id)
t1066_ds_UCh_w2$h_cpd <- car::recode(t1066_UCh_w2$COAD,"0=999;NA=999")

```


#### Statistics of the new harmonized variable

```{r descript UCh_w2, echo=F}

vbl <- t1066_ds_UCh_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_UCh_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation UCh_w2, echo=F}

AfterH <- table(t1066_ds_UCh_w2$h_cpd)
X <- table(t1066_UCh_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)],na.rm=T))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Rural China


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-RuralChina-Baseline` | 
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign RCh_w1, echo=F}

opal.assign.table.tibble(o, 't1066_RCh_w1','10-66.1066-RuralChina-Baseline',variables=list('COAD'), missings = T)
t1066_RCh_w1 <- opal.execute(o,'t1066_RCh_w1')

```

```{r local RCh_w1, echo=F}

vbl <- t1066_RCh_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_RCh_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_RCh_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo RCh_w1}

t1066_ds_RCh_w1 <- tibble(id=t1066_RCh_w1$id)
t1066_ds_RCh_w1$h_cpd <- car::recode(t1066_RCh_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript RCh_w1, echo=F}

vbl <- t1066_ds_RCh_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_RCh_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation RCh_w1, echo=F}

AfterH <- table(t1066_ds_RCh_w1$h_cpd)
BeforeH <- table(t1066_RCh_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-RuralChina-Baseline` | 
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign RCh_w2, echo=F}

opal.assign.table.tibble(o, 't1066_RCh_w2','10-66.1066-RuralChina-Baseline',variables=list('COAD'), missings = T)
t1066_RCh_w2 <- opal.execute(o,'t1066_RCh_w2')
opal.assign.table.tibble(o, 't1066_RCh_w2_id','10-66.1066-RuralChina-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_RCh_w2_id <- opal.execute(o,'t1066_RCh_w2_id')

```

```{r local RCh_w2, echo=F}

t1066_RCh_w2 <- left_join(t1066_RCh_w2_id, t1066_RCh_w2, by = "id")[,c(1,3)]

vbl <- t1066_RCh_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_RCh_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_RCh_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo RCh_w2}

t1066_ds_RCh_w2 <- tibble(id=t1066_RCh_w2$id)
t1066_ds_RCh_w2$h_cpd <- car::recode(t1066_RCh_w2$COAD,"NA=999;0=999")

```


#### Statistics of the new harmonized variable

```{r descript RCh_w2, echo=F}

vbl <- t1066_ds_RCh_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_RCh_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation RCh_w2, echo=F}

AfterH <- table(t1066_ds_RCh_w2$h_cpd)
X <- table(t1066_RCh_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)],na.rm=T))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Dominican Republic


### Wave 1


#### Study-specific variable description

| **Name** | `COAD`|
|-|-|-|
| **Label** | `Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  |`1066-DominicanRepublic-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign DR_w1, echo=F}

opal.assign.table.tibble(o, 't1066_DR_w1','10-66.1066-DominicanRepublic-Baseline',variables=list('COAD'), missings = T)
t1066_DR_w1 <- opal.execute(o,'t1066_DR_w1')

```

```{r local DR_w1, echo=F}

vbl <- t1066_DR_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_DR_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_DR_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo DR_w1}

t1066_ds_DR_w1 <- tibble(id=t1066_DR_w1$id)
t1066_ds_DR_w1$h_cpd <- car::recode(t1066_DR_w1$COAD,"NA=999")
```


#### Statistics of the new harmonized variable

```{r descript DR_w1, echo=F}

vbl <- t1066_ds_DR_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_DR_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation DR_w1, echo=F}

AfterH <- table(t1066_ds_DR_w1$h_cpd)
BeforeH <- table(t1066_DR_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1","NA->999")
kable(C)


```



### Wave 2


#### Study-specific variable description

| **Name** | `COAD`|
|-|-|-|
| **Label** | `Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  |`1066-DominicanRepublic-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign DR_w2, echo=F}

opal.assign.table.tibble(o, 't1066_DR_w2','10-66.1066-DominicanRepublic-Baseline',variables=list('COAD'), missings = T)
t1066_DR_w2 <- opal.execute(o,'t1066_DR_w2')
opal.assign.table.tibble(o, 't1066_DR_w2_id','10-66.1066-DominicanRepublic-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_DR_w2_id <- opal.execute(o,'t1066_DR_w2_id')

```

```{r local DR_w2, echo=F}

t1066_DR_w2 <- left_join(t1066_DR_w2_id, t1066_DR_w2, by = "id")[,c(1,3)]

vbl <- t1066_DR_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_DR_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_DR_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo DR_w2}

t1066_ds_DR_w2 <- tibble(id=t1066_DR_w2$id)
t1066_ds_DR_w2$h_cpd <-car::recode(t1066_DR_w2$COAD,"NA=999;0=999")

```


#### Statistics of the new harmonized variable

```{r descript DR_w2, echo=F}

vbl <- t1066_ds_DR_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_DR_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation DR_w2, echo=F}

AfterH <- table(t1066_ds_DR_w2$h_cpd)
X <- table(t1066_DR_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)]))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Venezuela


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  |`1066-Venezuela-Baseline` |
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |


```{r assign Ven_w1, echo=F}

opal.assign.table.tibble(o, 't1066_Ven_w1','10-66.1066-Venezuela-Baseline',variables=list('COAD'), missings = T)
t1066_Ven_w1 <- opal.execute(o,'t1066_Ven_w1')

```

```{r local Ven_w1, echo=F}

vbl <- t1066_Ven_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_Ven_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_Ven_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo Ven_w1}

t1066_ds_Ven_w1 <- tibble(id=t1066_Ven_w1$id)
t1066_ds_Ven_w1$h_cpd <- car::recode(t1066_Ven_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript Ven_w1, echo=F}

vbl <- t1066_ds_Ven_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_Ven_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation Ven_w1, echo=F}

AfterH <- table(t1066_ds_Ven_w1$h_cpd)
BeforeH <- table(t1066_Ven_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1","NA->999")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  |`1066-Venezuela-Baseline` |
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |


```{r assign Ven_w2, echo=F}

opal.assign.table.tibble(o, 't1066_Ven_w2','10-66.1066-Venezuela-Baseline',variables=list('COAD'), missings = T)
t1066_Ven_w2 <- opal.execute(o,'t1066_Ven_w2')
opal.assign.table.tibble(o, 't1066_Ven_w2_id','10-66.1066-Venezuela-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_Ven_w2_id <- opal.execute(o,'t1066_Ven_w2_id')

```

```{r local Ven_w2, echo=F}

t1066_Ven_w2 <- left_join(t1066_Ven_w2_id, t1066_Ven_w2, by = "id")[,c(1,3)]

vbl <- t1066_Ven_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_Ven_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_Ven_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo Ven_w2}

t1066_ds_Ven_w2 <- tibble(id=t1066_Ven_w2$id)
t1066_ds_Ven_w2$h_cpd <- car::recode(t1066_Ven_w2$COAD,"NA=999;0=999")

```


#### Statistics of the new harmonized variable

```{r descript Ven_w2, echo=F}

vbl <- t1066_ds_Ven_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_Ven_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation Ven_w2, echo=F}

AfterH <- table(t1066_ds_Ven_w2$h_cpd)
X <- table(t1066_Ven_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)]))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Urban Peru


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-UrbanPeru-Baseline` |
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign UP_w1, echo=F}

opal.assign.table.tibble(o, 't1066_UP_w1','10-66.1066-UrbanPeru-Baseline',variables=list('COAD'), missings = T)
t1066_UP_w1 <- opal.execute(o,'t1066_UP_w1')

```

```{r local UP_w1, echo=F}

vbl <- t1066_UP_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_UP_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_UP_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo UP_w1}

t1066_ds_UP_w1 <- tibble(id=t1066_UP_w1$id)
t1066_ds_UP_w1$h_cpd <- car::recode(t1066_UP_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript UP_w1, echo=F}

vbl <- t1066_ds_UP_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_UP_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation UP_w1, echo=F}

AfterH <- table(t1066_ds_UP_w1$h_cpd)
BeforeH <- table(t1066_UP_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1","NA->999")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-UrbanPeru-Baseline` |
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |


```{r assign UP_w2, echo=F}

opal.assign.table.tibble(o, 't1066_UP_w2','10-66.1066-UrbanPeru-Baseline',variables=list('COAD'), missings = T)
t1066_UP_w2 <- opal.execute(o,'t1066_UP_w2')
opal.assign.table.tibble(o, 't1066_UP_w2_id','10-66.1066-UrbanPeru-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_UP_w2_id <- opal.execute(o,'t1066_UP_w2_id')
```

```{r local UP_w2, echo=F}

t1066_UP_w2 <- left_join(t1066_UP_w2_id, t1066_UP_w2, by = "id")[,c(1,3)]

vbl <- t1066_UP_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_UP_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_UP_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo UP_w2}

t1066_ds_UP_w2 <- tibble(id=t1066_UP_w2$id)
t1066_ds_UP_w2$h_cpd <- car::recode(t1066_UP_w2$COAD,"NA=999;0=999")

```


#### Statistics of the new harmonized variable

```{r descript UP_w2, echo=F}

vbl <- t1066_ds_UP_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_UP_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation UP_w2, echo=F}

AfterH <- table(t1066_ds_UP_w2$h_cpd)
X <- table(t1066_UP_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)]))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Rural Peru


### Wave 1


#### Study-specific variable description

| **Name** | `COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-RuralPeru-Baseline` |
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign RP_w1, echo=F}

opal.assign.table.tibble(o, 't1066_RP_w1','10-66.1066-RuralPeru-Baseline',variables=list('COAD'), missings = T)
t1066_RP_w1 <- opal.execute(o,'t1066_RP_w1')

```

```{r local RP_w1, echo=F}

vbl <- t1066_RP_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_RP_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_RP_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo RP_w1}

t1066_ds_RP_w1 <- tibble(id=t1066_RP_w1$id)
t1066_ds_RP_w1$h_cpd <- car::recode(t1066_RP_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript RP_w1, echo=F}

vbl <- t1066_ds_RP_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_RP_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation RP_w1, echo=F}

AfterH <- table(t1066_ds_RP_w1$h_cpd)
BeforeH <- table(t1066_RP_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1","NA->999")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** | `COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-RuralPeru-Baseline` |
| **Categories** | `0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign RP_w2, echo=F}

opal.assign.table.tibble(o, 't1066_RP_w2','10-66.1066-RuralPeru-Baseline',variables=list('COAD'), missings = T)
t1066_RP_w2 <- opal.execute(o,'t1066_RP_w2')
opal.assign.table.tibble(o, 't1066_RP_w2_id','10-66.1066-RuralPeru-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_RP_w2_id <- opal.execute(o,'t1066_RP_w2_id')

```

```{r local RP_w2, echo=F}

t1066_RP_w2 <- left_join(t1066_RP_w2_id, t1066_RP_w2, by = "id")[,c(1,3)]

vbl <- t1066_RP_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_RP_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_RP_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo RP_w2}

t1066_ds_RP_w2 <- tibble(id=t1066_RP_w2$id)
t1066_ds_RP_w2$h_cpd <- car::recode(t1066_RP_w2$COAD,"NA=999;0=999")


```


#### Statistics of the new harmonized variable

```{r descript RP_w2, echo=F}

vbl <- t1066_ds_RP_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_RP_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation RP_w2, echo=F}

AfterH <- table(t1066_ds_RP_w2$h_cpd)
X <- table(t1066_RP_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)]))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Urban Mexico


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-UrbanMexico-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign UM_w1, echo=F}

opal.assign.table.tibble(o, 't1066_UM_w1','10-66.1066-UrbanMexico-Baseline',variables=list('COAD'), missings = T)
t1066_UM_w1 <- opal.execute(o,'t1066_UM_w1')

```

```{r local UM_w1, echo=F}

vbl <- t1066_UM_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_UM_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_UM_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo UM_w1}

t1066_ds_UM_w1 <- tibble(id=t1066_UM_w1$id)
t1066_ds_UM_w1$h_cpd <- car::recode(t1066_UM_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript UM_w1, echo=F}

vbl <- t1066_ds_UM_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_UM_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation UM_w1, echo=F}

AfterH <- table(t1066_ds_UM_w1$h_cpd)
BeforeH <- table(t1066_UM_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-UrbanMexico-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign UM_w2, echo=F}

opal.assign.table.tibble(o, 't1066_UM_w2','10-66.1066-UrbanMexico-Baseline',variables=list('COAD'), missings = T)
t1066_UM_w2 <- opal.execute(o,'t1066_UM_w2')
opal.assign.table.tibble(o, 't1066_UM_w2_id','10-66.1066-UrbanMexico-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_UM_w2_id <- opal.execute(o,'t1066_UM_w2_id')

```

```{r local UM_w2, echo=F}

t1066_UM_w2 <- left_join(t1066_UM_w2_id, t1066_UM_w2, by = "id")[,c(1,3)]

vbl <- t1066_UM_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_UM_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_UM_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo UM_w2}

t1066_ds_UM_w2 <- tibble(id=t1066_UM_w2$id)
t1066_ds_UM_w2$h_cpd <- car::recode(t1066_UM_w2$COAD,"NA=999;0=999")

```


#### Statistics of the new harmonized variable

```{r descript UM_w2, echo=F}

vbl <- t1066_ds_UM_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_UM_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")


```


#### Validation

```{r crosstabulation UM_w2, echo=F}

AfterH <- table(t1066_ds_UM_w2$h_cpd)
X <- table(t1066_UM_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)],na.rm=T))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Rural Mexico


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  |  `1066-RuralMexico-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign RM_w1, echo=F}

opal.assign.table.tibble(o, 't1066_RM_w1','10-66.1066-RuralMexico-Baseline',variables=list('COAD'), missings = T)
t1066_RM_w1 <- opal.execute(o,'t1066_RM_w1')

```

```{r local RM_w1, echo=F}

vbl <- t1066_RM_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_RM_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_RM_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo RM_w1}

t1066_ds_RM_w1 <- tibble(id=t1066_RM_w1$id)
t1066_ds_RM_w1$h_cpd <- car::recode(t1066_RM_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript RM_w1, echo=F}

vbl <- t1066_ds_RM_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_RM_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation RM_w1, echo=F}

AfterH <- table(t1066_ds_RM_w1$h_cpd)
BeforeH <- table(t1066_RM_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  |  `1066-RuralMexico-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign RM_w2, echo=F}

opal.assign.table.tibble(o, 't1066_RM_w2','10-66.1066-RuralMexico-Baseline',variables=list('COAD'), missings = T)
t1066_RM_w2 <- opal.execute(o,'t1066_RM_w2')
opal.assign.table.tibble(o, 't1066_RM_w2_id','10-66.1066-RuralMexico-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_RM_w2_id <- opal.execute(o,'t1066_RM_w2_id')

```

```{r local RM_w2, echo=F}

t1066_RM_w2 <- left_join(t1066_RM_w2_id, t1066_RM_w2, by = "id")[,c(1,3)]

vbl <- t1066_RM_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_RM_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_RM_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo RM_w2}

t1066_ds_RM_w2 <- tibble(id=t1066_RM_w2$id)
t1066_ds_RM_w2$h_cpd <- car::recode(t1066_RM_w2$COAD,"NA=999;0=999")

```


#### Statistics of the new harmonized variable

```{r descript RM_w2, echo=F}

vbl <- t1066_ds_RM_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_RM_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation RM_w2, echo=F}

AfterH <- table(t1066_ds_RM_w2$h_cpd)
X <- table(t1066_RM_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)],na.rm=T))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```



## Puerto Rico


### Wave 1


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-PuertoRico-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign PR_w1, echo=F}

opal.assign.table.tibble(o, 't1066_PR_w1','10-66.1066-PuertoRico-Baseline',variables=list('COAD'), missings = T)
t1066_PR_w1 <- opal.execute(o,'t1066_PR_w1')

```

```{r local PR_w1, echo=F}

vbl <- t1066_PR_w1$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_PR_w1)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_PR_w1, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 0 into 0`
* `COAD ==  1 into 1`
* `NA into 999`

**R script:**

```{r harmo PR_w1}

t1066_ds_PR_w1 <- tibble(id=t1066_PR_w1$id)
t1066_ds_PR_w1$h_cpd <- car::recode(t1066_PR_w1$COAD,"NA=999")

```


#### Statistics of the new harmonized variable

```{r descript PR_w1, echo=F}

vbl <- t1066_ds_PR_w1$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_PR_w1, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation PR_w1, echo=F}

AfterH <- table(t1066_ds_PR_w1$h_cpd)
BeforeH <- table(t1066_PR_w1$COAD, useNA = "ifany")
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("0->0","1->1","NA->999")
kable(C)

```



### Wave 2


#### Study-specific variable description

| **Name** |`COAD`|
|-|-|-|
| **Label** |`Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year`|
| **Table name**  | `1066-PuertoRico-Baseline` |
| **Categories** |`0 = does not met criteria for COPD`<br/>`1 = meets criteria for COPD`|
| **Missings** | `NA` |
| **Description** |  |

```{r assign PR_w2, echo=F}

opal.assign.table.tibble(o, 't1066_PR_w2','10-66.1066-PuertoRico-Baseline',variables=list('COAD'), missings = T)
t1066_PR_w2 <- opal.execute(o,'t1066_PR_w2')
opal.assign.table.tibble(o, 't1066_PR_w2_id','10-66.1066-PuertoRico-Followup',variables=list('HOUSEID'), missings = TRUE)
t1066_PR_w2_id <- opal.execute(o,'t1066_PR_w2_id')

```

```{r local PR_w2, echo=F}

t1066_PR_w2 <- left_join(t1066_PR_w2_id, t1066_PR_w2, by = "id")[,c(1,3)]

vbl <- t1066_PR_w2$COAD
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(t1066_PR_w2)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(t1066_PR_w2, aes(x=factor(COAD))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `h_cpd` from the study-specific variable it has to be recoded as follows:

* `COAD == 1 into 1`
* `otherwise into 999`

**R script:**

```{r harmo PR_w2}

t1066_ds_PR_w2 <- tibble(id=t1066_PR_w2$id)
t1066_ds_PR_w2$h_cpd <- car::recode(t1066_PR_w2$COAD,"NA=999;0=999")

```


#### Statistics of the new harmonized variable

```{r descript PR_w2, echo=F}

vbl <- t1066_ds_PR_w2$h_cpd
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(t1066_ds_PR_w2, aes(x=factor(h_cpd))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

```


#### Validation

```{r crosstabulation PR_w2, echo=F}

AfterH <- table(t1066_ds_PR_w2$h_cpd)
X <- table(t1066_PR_w2$COAD,useNA = "ifany")
BeforeH <- c(X[2],sum(X[c(1,3)]))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("1->1","0,NA->999")
kable(C)

```






<!--AUXILIAR FUNCTIONS-->



```{r helpfunctions, echo=F}


labelling <- function(l.hds,m.hds,vbl_name,ds_lab = ds_label){
  
  # Labelling of the tibbles with categorical data and creating new tibbles with all missings recodified as NA

  for(name in names(l.hds)) {
    if(vbl_name %in% names(l.hds[[1]])){
      # In the aux_object we copy the old tibble to recodify all missing values as NA.
      aux_object <- l.hds[[name]]
      # Labelling of variables
      label(l.hds[[name]][[vbl_name]]) <- label(aux_object[[vbl_name]]) <- ds_lab
      # Labelling of categories (for continues variables, only missing values)
      l.hds[[name]][[vbl_name]] <- labelled(l.hds[[name]][[vbl_name]], labels = cat_label)
      aux_object[[vbl_name]] <- car::recode(aux_object[[vbl_name]], "miss_values_vector=NA")
      # Labelling of categories (for categorical variables, only non-missing values)
      aux_object[[vbl_name]] <- labelled(aux_object[[vbl_name]], labels = cat_label[1:(length(cat_label)-9)])
      # Saving the recodified tibble in list m.hds
      m.hds[[name]] <- aux_object
      rm(aux_object)
    }
  }
  return(list(l.hds,m.hds))

}


labelling_c <- function(l.hds,m.hds,vbl_name,ds_lab=ds_label){
  
  # Labelling of the tibbles with continuous data and creating new tibbles with all missings recodified as NA

  for(name in names(l.hds)) {
    if(vbl_name %in% names(l.hds[[1]])){
      # In the aux_object we copy the old tibble to recodify all missing values as NA.
      aux_object <- l.hds[[name]]
      # Labelling of variables
      label(l.hds[[name]][[vbl_name]]) <- label(aux_object[[vbl_name]]) <- ds_lab
      # Labelling of categories (for continues variables, only missing values)
      l.hds[[name]][[vbl_name]] <- labelled(l.hds[[name]][[vbl_name]], labels = cont_label)
      aux_object[[vbl_name]] <- car::recode(aux_object[[vbl_name]], "miss_values_vector=NA")
      # Saving the recodified tibble in list m.hds
      m.hds[[name]] <- aux_object
      rm(aux_object)
    }
  }
  return(list(l.hds,m.hds))

}


# Creation of summary tables for categorical data

summaries <- function(l.hds,m.hds,lnames,vbl_name){

  # Creation of columns with categories and labels
  t.hds <- frq(l.hds[[1]][vbl_name])[[1]][,c(1,2)] 
  # For each wave/population in l.hds, add the correponding values
  for (i in seq_along(l.hds)){
    t.hds[2+i] <- frq(l.hds[[i]][vbl_name])[[1]][,4] 
  }
  # Add sample size for each wave/population
  t.hds[2+length(cat_label),] <- c("n", "sample size", sapply(l.hds,function(wave) length(wave[[1]]))
  )
  # Add wave/population names
  names(t.hds)<-c("val", "label",lnames)
  return(t.hds)
  
}


# Creation of summary tables for continuous data

summaries_c <- function(l.hds,m.hds,lnames,vbl_name){

  # Creation of column with summary table categories
  t.summ <- summary(m.hds[[1]][vbl_name])[1:6]
  # Adding of missing/no-missing values categories
  t.hds <- c(substr(t.summ,1,regexpr(":", t.summ, fixed=T)-1),labels(Continuous_summary(l.hds[[1]][[vbl_name]], missing_values = miss_values_vector)$values_table)[[2]])
  # For each wave/population in l.hds, add the correponding values
  for (i in seq_along(l.hds)){
    # First, summary values
    t.summ <- summary(m.hds[[i]][vbl_name])[1:6]
    # Next, missing/no-missing values (the 1 in $values_table[1,] stands for absolute values, while a 2 would stand for percentages)
    t.hds <- cbind(t.hds,c(as.numeric(substr(t.summ,regexpr(":", t.summ, fixed=T)+1,nchar(t.summ))),as.numeric(Continuous_summary(l.hds[[i]][[vbl_name]],missing_values = miss_values_vector)$values_table[1,])))
  }
  # Add sample size for each wave/population
  t.hds <- rbind(t.hds,c("sample size", sapply(l.hds,function(wave) length(wave[[1]]))))
  # Add wave/population names
  dimnames(t.hds)[[2]] <- c(dimnames(summary(m.hds[[1]][vbl_name]))[[2]],lnames)
  return(t.hds)
  
}


# Save data tables

savingRD <- function(l.hds,vbl_name){
  
  for(index in seq_along(l.hds)){
    assign(vbl_name,l.hds[[index]])
    save(vbl_name,list = vbl_name, file = paste0(datafolder,names(l.hds)[index],"/",vbl_name,".RData"))
  }
  
}

```



```{r populations-waves, echo=F}


# All study waves and populations with abbreviated and descriptive names

b1066.cw <- list(cuba_w1 = c("cuba_w1","Cuba w1"), india_w1 = c("india_w1","India w1"), UCh_w1 = c("UCh_w1","Urban China w1"), RCh_w1 = c("RCh_w1","Rural China w1"), DR_w1 = c("DR_w1","Dominican Republic w1"), Ven_w1 = c("Ven_w1","Venezuela w1"), UP_w1 = c("UP_w1","Urban Peru w1"), RP_w1 = c("RP_w1","Rural Peru w1"), UM_w1 = c("UM_w1","Urban Mexico w1"), RM_w1 = c("RM_w1","Rural Mexico w1"), PR_w1 = c("PR_w1","Puerto Rico w1"))

f1066.cw <- list(cuba_w2 = c("cuba_w2","Cuba w2"), UCh_w2 = c("UCh_w2","Urban China w2"), RCh_w2 = c("RCh_w2","Rural China w2"), DR_w2 = c("DR_w2","Dominican Republic w2"), Ven_w2 = c("Ven_w2","Venezuela w2"), UP_w2 = c("UP_w2","Urban Peru w2"), RP_w2 = c("RP_w2","Rural Peru w2"), UM_w2 = c("UM_w2","Urban Mexico w2"), RM_w2 = c("RM_w2","Rural Mexico w2"), PR_w2 = c("PR_w2","Puerto Rico w2"))


```



## Descriptive statistics of the harmonised variable `h_cpd` accross populations and waves

Percentages of categories in each harmonised variable:

```{r, echo=FALSE}


# Consider only waves with some variable harmonised

l.hds <- lapply(b1066.cw, function(wname) if(exists(paste0("t1066_ds_",wname[1]))){wname = list(get(paste0("t1066_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)

if(length(l.hds)>0){
  # Labelling categorical variable of tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]],hd_vbl,ds_label)
  # Printing table of categories
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]]),hd_vbl),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}



# Consider only waves with some variable harmonised

l.hds <- lapply(f1066.cw, function(wname) if(exists(paste0("t1066_ds_",wname[1]))){wname = list(get(paste0("t1066_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)


if(length(l.hds)>0){
  # Labelling categorical variable of tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]],hd_vbl,ds_label)
  # Printing table of categories
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]]),hd_vbl),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}



```



# Quality estimation

* Information is about "Chronic Obstructive Pulmonary Disease (COPD) - cough productive of sputum for at least 3 months per year".
* Age of diagnosis cannot be harmonised.





<!-- ########################################################## -->
<!--- # Close OPAL R Session -->
```{r closeRsession, echo=FALSE}
opal.logout(o)
```





