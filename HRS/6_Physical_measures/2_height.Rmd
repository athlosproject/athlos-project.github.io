---
title: "Height"
output:
  html_document:
    highlight: tango
    number_sections: yes
    theme: united
    toc: yes
    toc_depth: 6
    toc_float:
      collapsed: yes
      smooth_scroll: yes
---

<!-- ########################################################## --> 
<!-- # Installation, loading packages -->
```{r setup, include=FALSE}
source("../../setup.r")
```
<!-- ########################################################## --> 
<!-- # Loading DS description -->
```{r main, child='../../_DS_Rmd/heightDS.Rmd'}
```
```{r global, echo=F}
source('../../_DS_Rmd/heightDS.R')
```



# Data process


## HRS

### Wave 1 

#### Study-specific variable description

| **Name** | `R1HEIGHT`|
|-|-|
| **Label** | `W1 Height in meters`|
| **Table name** |`HRS-HRS-w1`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs1, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w1','HRS.HRS-HRS-w1',
                         variables=list('R1HEIGHT'), missings = TRUE)
HRS_hrs_w1 <- opal.execute(o,'HRS_hrs_w1')
```

```{r local_hrs1, echo=F}

vbl <- HRS_hrs_w1$R1HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R1HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w1, aes(R1HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs1}
HRS_ds_hrs_w1 <- tibble(id=HRS_hrs_w1$id)
HRS_ds_hrs_w1$height <- car::recode(HRS_hrs_w1$R1HEIGHT*100, "NA=999")
HRS_ds_hrs_w1$height <- labelled(HRS_ds_hrs_w1$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs1, echo=F}
vbl <- HRS_ds_hrs_w1$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w1, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency")  
```

#### Validation

```{r crosstabulation_hrs1, echo=F}
kable(HRS_hrs_w1[which(HRS_hrs_w1$R1HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_hrs1, echo=F}






```







### Wave 2 

#### Study-specific variable description



| **Name** | `R2HEIGHT`|
|-|-|
| **Label** | `W2 Height in meters`|
| **Table name** |`HRS-HRS-w2`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs2, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w2','HRS.HRS-HRS-w2',
                         variables=list('R2HEIGHT'), missings = TRUE)
HRS_hrs_w2 <- opal.execute(o,'HRS_hrs_w2')
```

```{r local_hrs2, echo=F}

vbl <- HRS_hrs_w2$R2HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R2HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w2, aes(R2HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs2}
HRS_ds_hrs_w2 <- tibble(id=HRS_hrs_w2$id)
HRS_ds_hrs_w2$height <- car::recode(HRS_hrs_w2$R2HEIGHT*100, "NA=999")
HRS_ds_hrs_w2$height <- labelled(HRS_ds_hrs_w2$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs2, echo=F}
vbl <- HRS_ds_hrs_w2$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w2, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency")  
```

#### Validation

```{r crosstabulation_hrs2, echo=F}
kable(HRS_hrs_w2[which(HRS_hrs_w2$R2HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_hrs2, echo=F}






```







### Wave 3 

#### Study-specific variable description




| **Name** | `R3HEIGHT`|
|-|-|
| **Label** | `W3 Height in meters`|
| **Table name** |`HRS-HRS-w3`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs3, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w3','HRS.HRS-HRS-w3',
                         variables=list('R3HEIGHT'), missings = TRUE)
HRS_hrs_w3 <- opal.execute(o,'HRS_hrs_w3')
```

```{r local_hrs3, echo=F}

vbl <- HRS_hrs_w3$R3HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R3HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w3, aes(R3HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs3}
HRS_ds_hrs_w3 <- tibble(id=HRS_hrs_w3$id)
HRS_ds_hrs_w3$height <- car::recode(HRS_hrs_w3$R3HEIGHT*100, "NA=999")
HRS_ds_hrs_w3$height <- labelled(HRS_ds_hrs_w3$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs3, echo=F}
vbl <- HRS_ds_hrs_w3$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w3, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency")  
```

#### Validation
```{r crosstabulation_hrs3, echo=F}

```





```{r importOPAL_hrs3, echo=F}






```







### Wave 4 

#### Study-specific variable description



| **Name** | `R4HEIGHT`|
|-|-|
| **Label** | `W4 Height in meters`|
| **Table name** |`HRS-HRS-w4`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs4, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w4','HRS.HRS-HRS-w4',
                         variables=list('R4HEIGHT'), missings = TRUE)
HRS_hrs_w4 <- opal.execute(o,'HRS_hrs_w4')
```

```{r local_hrs4, echo=F}

vbl <- HRS_hrs_w4$R4HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R4HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w4, aes(R4HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs4}
HRS_ds_hrs_w4 <- tibble(id=HRS_hrs_w4$id)
HRS_ds_hrs_w4$height <- car::recode(HRS_hrs_w4$R4HEIGHT*100, "NA=999")
HRS_ds_hrs_w4$height <- labelled(HRS_ds_hrs_w4$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs4, echo=F}
vbl <- HRS_ds_hrs_w4$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w4, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency")  
```

#### Validation
```{r crosstabulation_hrs4, echo=F}

```





```{r importOPAL_hrs4, echo=F}






```








### Wave 5

#### Study-specific variable description



| **Name** | `R5HEIGHT`|
|-|-|
| **Label** | `W5 Height in meters`|
| **Table name** |`HRS-HRS-w5`|
| **Units** |`m`|
| **Description** |  |

```{r assign_hrs5, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w5','HRS.HRS-HRS-w5',
                         variables=list('R5HEIGHT'), missings = TRUE)
HRS_hrs_w5 <- opal.execute(o,'HRS_hrs_w5')
```

```{r local_hrs5, echo=F}

vbl <- HRS_hrs_w5$R5HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R5HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w5, aes(R5HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs5}
HRS_ds_hrs_w5 <- tibble(id=HRS_hrs_w5$id)
HRS_ds_hrs_w5$height <- car::recode(HRS_hrs_w5$R5HEIGHT*100, "NA=999")
HRS_ds_hrs_w5$height <- labelled(HRS_ds_hrs_w5$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs5, echo=F}
vbl <- HRS_ds_hrs_w5$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w5, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_hrs5, echo=F}

```





```{r importOPAL_hrs5, echo=F}






```






### Wave 6

#### Study-specific variable description



| **Name** | `R6HEIGHT`|
|-|-|
| **Label** | `W6 Height in meters`|
| **Table name** |`HRS-HRS-w6`|
| **Units** |`m`|
| **Description** |  |

```{r assign_hrs6, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w6','HRS.HRS-HRS-w6',
                         variables=list('R6HEIGHT'), missings = TRUE)
HRS_hrs_w6 <- opal.execute(o,'HRS_hrs_w6')
```

```{r local_hrs6, echo=F}

vbl <- HRS_hrs_w6$R6HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R6HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w6, aes(R6HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs6}
HRS_ds_hrs_w6 <- tibble(id=HRS_hrs_w6$id)
HRS_ds_hrs_w6$height <- car::recode(HRS_hrs_w6$R6HEIGHT*100, "NA=999")
HRS_ds_hrs_w6$height <- labelled(HRS_ds_hrs_w6$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs6, echo=F}
vbl <- HRS_ds_hrs_w6$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w6, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_hrs6, echo=F}
```





```{r importOPAL_hrs6, echo=F}






```








### Wave 7

#### Study-specific variable description



| **Name** | `R7HEIGHT`|
|-|-|
| **Label** | `W7 Height in meters`|
| **Table name** |`HRS-HRS-w7`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs7, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w7','HRS.HRS-HRS-w7',
                         variables=list('R7HEIGHT'), missings = TRUE)
HRS_hrs_w7 <- opal.execute(o,'HRS_hrs_w7')
```

```{r local_hrs7, echo=F}

vbl <- HRS_hrs_w7$R7HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R7HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w7, aes(R7HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs7}
HRS_ds_hrs_w7 <- tibble(id=HRS_hrs_w7$id)
HRS_ds_hrs_w7$height <- car::recode(HRS_hrs_w7$R7HEIGHT*100, "NA=999")
HRS_ds_hrs_w7$height <- labelled(HRS_ds_hrs_w7$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs7, echo=F}
vbl <- HRS_ds_hrs_w7$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w7, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_hrs7, echo=F}
```





```{r importOPAL_hrs7, echo=F}






```








### Wave 8

#### Study-specific variable description



| **Name** | `R8HEIGHT`|
|-|-|
| **Label** | `W8 Height in meters`|
| **Table name** |`HRS-HRS-w8`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs8, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w8','HRS.HRS-HRS-w8',
                         variables=list('R8HEIGHT'), missings = TRUE)
HRS_hrs_w8 <- opal.execute(o,'HRS_hrs_w8')
```

```{r local_hrs8, echo=F}

vbl <- HRS_hrs_w8$R8HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R8HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w8, aes(R8HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs8}
HRS_ds_hrs_w8 <- tibble(id=HRS_hrs_w8$id)
HRS_ds_hrs_w8$height <- car::recode(HRS_hrs_w8$R8HEIGHT*100, "NA=999")
HRS_ds_hrs_w8$height <- labelled(HRS_ds_hrs_w8$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs8, echo=F}
vbl <- HRS_ds_hrs_w8$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w8, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_hrs8, echo=F}

```





```{r importOPAL_hrs8, echo=F}






```








### Wave 9

#### Study-specific variable description



| **Name** | `R9HEIGHT`|
|-|-|
| **Label** | `W9 Height in meters`|
| **Table name** |`HRS-HRS-w9`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs9, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w9','HRS.HRS-HRS-w9',
                         variables=list('R9HEIGHT'), missings = TRUE)
HRS_hrs_w9 <- opal.execute(o,'HRS_hrs_w9')
```

```{r local_hrs9, echo=F}

vbl <- HRS_hrs_w9$R9HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R9HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w9, aes(R9HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`


**R script:**

```{r harmo_hrs9}
HRS_ds_hrs_w9 <- tibble(id=HRS_hrs_w9$id)
HRS_ds_hrs_w9$height <- car::recode(HRS_hrs_w9$R9HEIGHT*100, "NA=999")
HRS_ds_hrs_w9$height <- labelled(HRS_ds_hrs_w9$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs9, echo=F}
vbl <- HRS_ds_hrs_w9$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w9, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_hrs9, echo=F}

```





```{r importOPAL_hrs9, echo=F}






```







### Wave 10

#### Study-specific variable description



| **Name** | `R10HEIGHT`|
|-|-|
| **Label** | `W10 Height in meters`|
| **Table name** |`HRS-HRS-w10`|
| **Units** |`m`|
| **Description** |  |


```{r assign_hrs10, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w10','HRS.HRS-HRS-w10',
                         variables=list('R10HEIGHT'), missings = TRUE)
HRS_hrs_w10 <- opal.execute(o,'HRS_hrs_w10')
```

```{r local_hrs10, echo=F}

vbl <- HRS_hrs_w10$R10HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R10HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w10, aes(R10HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs10}
HRS_ds_hrs_w10 <- tibble(id=HRS_hrs_w10$id)
HRS_ds_hrs_w10$height <- car::recode(HRS_hrs_w10$R10HEIGHT*100, "NA=999")
HRS_ds_hrs_w10$height <- labelled(HRS_ds_hrs_w10$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs10, echo=F}
vbl <- HRS_ds_hrs_w10$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w10, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_hrs10, echo=F}

```





```{r importOPAL_hrs10, echo=F}






```







### Wave 11

#### Study-specific variable description



| **Name** | `R11HEIGHT`|
|-|-|
| **Label** | `W11 Height in meters`|
| **Table name** |`HRS-HRS-w11`|
| **Units** |`m`|
| **Description** |  |

```{r assign_hrs11, echo=F}
opal.assign.table.tibble(o, 'HRS_hrs_w11','HRS.HRS-HRS-w11',
                         variables=list('R11HEIGHT'), missings = TRUE)
HRS_hrs_w11 <- opal.execute(o,'HRS_hrs_w11')
```

```{r local_hrs11, echo=F}

vbl <- HRS_hrs_w11$R11HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R11HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_hrs_w11, aes(R11HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_hrs11}
HRS_ds_hrs_w11 <- tibble(id=HRS_hrs_w11$id)
HRS_ds_hrs_w11$height <- car::recode(HRS_hrs_w11$R11HEIGHT*100, "NA=999")
HRS_ds_hrs_w11$height <- labelled(HRS_ds_hrs_w11$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_hrs11, echo=F}
vbl <- HRS_ds_hrs_w11$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_hrs_w11, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_hrs11, echo=F}

```





```{r importOPAL_hrs11, echo=F}






```





## AHEAD


### Wave 2 

#### Study-specific variable description



| **Name** | `R2HEIGHT`|
|-|-|
| **Label** | `W2 Height in meters`|
| **Table name** |`HRS-AHEAD-w2`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead2, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w2','HRS.HRS-AHEAD-w2',
                         variables=list('R2HEIGHT'), missings = TRUE)
HRS_ahead_w2 <- opal.execute(o,'HRS_ahead_w2')
```

```{r local_ahead2, echo=F}

vbl <- HRS_ahead_w2$R2HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R2HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w2, aes(R2HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead2}
HRS_ds_ahead_w2 <- tibble(id=HRS_ahead_w2$id)
HRS_ds_ahead_w2$height <- car::recode(HRS_ahead_w2$R2HEIGHT*100, "NA=999")
HRS_ds_ahead_w2$height <- labelled(HRS_ds_ahead_w2$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead2, echo=F}
vbl <- HRS_ds_ahead_w2$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w2, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead2, echo=F}
kable(HRS_ahead_w2[which(HRS_ahead_w2$R2HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```






```{r importOPAL_ahead2, echo=F}






```





### Wave 3 

#### Study-specific variable description



| **Name** | `R3HEIGHT`|
|-|-|
| **Label** | `W3 Height in meters`|
| **Table name** |`HRS-AHEAD-w3`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead3, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w3','HRS.HRS-AHEAD-w3',
                         variables=list('R3HEIGHT'), missings = TRUE)
HRS_ahead_w3 <- opal.execute(o,'HRS_ahead_w3')
```

```{r local_ahead3, echo=F}

vbl <- HRS_ahead_w3$R3HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R3HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w3, aes(R3HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`


**R script:**

```{r harmo_ahead3}
HRS_ds_ahead_w3 <- tibble(id=HRS_ahead_w3$id)
HRS_ds_ahead_w3$height <- car::recode(HRS_ahead_w3$R3HEIGHT*100, "NA=999")
HRS_ds_ahead_w3$height <- labelled(HRS_ds_ahead_w3$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead3, echo=F}
vbl <- HRS_ds_ahead_w3$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w3, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead3, echo=F}
kable(HRS_ahead_w2[which(HRS_ahead_w3$R3HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_ahead3, echo=F}






```






### Wave 4 

#### Study-specific variable description



| **Name** | `R4HEIGHT`|
|-|-|
| **Label** | `W4 Height in meters`|
| **Table name** |`HRS-AHEAD-w4`|
| **Units** |`m`|
| **Description** |  |

```{r assign_ahead4, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w4','HRS.HRS-AHEAD-w4',
                         variables=list('R4HEIGHT'), missings = TRUE)
HRS_ahead_w4 <- opal.execute(o,'HRS_ahead_w4')
```

```{r local_ahead4, echo=F}

vbl <- HRS_ahead_w4$R4HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R4HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w4, aes(R4HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead4}
HRS_ds_ahead_w4 <- tibble(id=HRS_ahead_w4$id)
HRS_ds_ahead_w4$height <- car::recode(HRS_ahead_w4$R4HEIGHT*100, "NA=999")
HRS_ds_ahead_w4$height <- labelled(HRS_ds_ahead_w4$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead4, echo=F}
vbl <- HRS_ds_ahead_w4$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w4, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead4, echo=F}
kable(HRS_ahead_w4[which(HRS_ahead_w4$R4HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_ahead4, echo=F}






```







### Wave 5 

#### Study-specific variable description



| **Name** | `R5HEIGHT`|
|-|-|
| **Label** | `W5 Height in meters`|
| **Table name** |`HRS-AHEAD-w5`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead5, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w5','HRS.HRS-AHEAD-w5',
                         variables=list('R5HEIGHT'), missings = TRUE)
HRS_ahead_w5 <- opal.execute(o,'HRS_ahead_w5')
```

```{r local_ahead5, echo=F}

vbl <- HRS_ahead_w5$R5HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R5HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w5, aes(R5HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead5}
HRS_ds_ahead_w5 <- tibble(id=HRS_ahead_w5$id)
HRS_ds_ahead_w5$height <- car::recode(HRS_ahead_w5$R5HEIGHT*100, "NA=999")
HRS_ds_ahead_w5$height <- labelled(HRS_ds_ahead_w5$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead5, echo=F}
vbl <- HRS_ds_ahead_w5$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w5, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead5, echo=F}
kable(HRS_ahead_w5[which(HRS_ahead_w5$R5HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_ahead5, echo=F}






```








### Wave 6 

#### Study-specific variable description



| **Name** | `R6HEIGHT`|
|-|-|
| **Label** | `W6 Height in meters`|
| **Table name** |`HRS-AHEAD-w6`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead6, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w6','HRS.HRS-AHEAD-w6',
                         variables=list('R6HEIGHT'), missings = TRUE)
HRS_ahead_w6 <- opal.execute(o,'HRS_ahead_w6')
```

```{r local_ahead6, echo=F}

vbl <- HRS_ahead_w6$R6HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R6HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w6, aes(R6HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead6}
HRS_ds_ahead_w6 <- tibble(id=HRS_ahead_w6$id)
HRS_ds_ahead_w6$height <- car::recode(HRS_ahead_w6$R6HEIGHT*100, "NA=999")
HRS_ds_ahead_w6$height <- labelled(HRS_ds_ahead_w6$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead6, echo=F}
vbl <- HRS_ds_ahead_w6$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w6, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead6, echo=F}
kable(HRS_ahead_w6[which(HRS_ahead_w6$R6HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_ahead6, echo=F}






```







### Wave 7 

#### Study-specific variable description



| **Name** | `R7HEIGHT`|
|-|-|
| **Label** | `W7 Height in meters`|
| **Table name** |`HRS-AHEAD-w7`|
| **Units** |`m`|
| **Description** |  |

```{r assign_ahead7, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w7','HRS.HRS-AHEAD-w7',
                         variables=list('R7HEIGHT'), missings = TRUE)
HRS_ahead_w7 <- opal.execute(o,'HRS_ahead_w7')
```

```{r local_ahead7, echo=F}

vbl <- HRS_ahead_w7$R7HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R7HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w7, aes(R7HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead7}
HRS_ds_ahead_w7 <- tibble(id=HRS_ahead_w7$id)
HRS_ds_ahead_w7$height <- car::recode(HRS_ahead_w7$R7HEIGHT*100, "NA=999")
HRS_ds_ahead_w7$height <- labelled(HRS_ds_ahead_w7$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead7, echo=F}
vbl <- HRS_ds_ahead_w7$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w7, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead7, echo=F}
kable(HRS_ahead_w7[which(HRS_ahead_w7$R7HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_ahead7, echo=F}






```








### Wave 8 

#### Study-specific variable description



| **Name** | `R8HEIGHT`|
|-|-|
| **Label** | `W8 Height in meters`|
| **Table name** |`HRS-AHEAD-w8`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead8, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w8','HRS.HRS-AHEAD-w8',
                         variables=list('R8HEIGHT'), missings = TRUE)
HRS_ahead_w8 <- opal.execute(o,'HRS_ahead_w8')
```

```{r local_ahead8, echo=F}

vbl <- HRS_ahead_w8$R8HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R8HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w8, aes(R8HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead8}
HRS_ds_ahead_w8 <- tibble(id=HRS_ahead_w8$id)
HRS_ds_ahead_w8$height <- car::recode(HRS_ahead_w8$R8HEIGHT*100, "NA=999")
HRS_ds_ahead_w8$height <- labelled(HRS_ds_ahead_w8$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead8, echo=F}
vbl <- HRS_ds_ahead_w8$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w8, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_ahead8, echo=F}

```





```{r importOPAL_ahead8, echo=F}






```








### Wave 9 

#### Study-specific variable description



| **Name** | `R9HEIGHT`|
|-|-|
| **Label** | `W9 Height in meters`|
| **Table name** |`HRS-AHEAD-w9`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead9, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w9','HRS.HRS-AHEAD-w9',
                         variables=list('R9HEIGHT'), missings = TRUE)
HRS_ahead_w9 <- opal.execute(o,'HRS_ahead_w9')
```

```{r local_ahead9, echo=F}

vbl <- HRS_ahead_w9$R9HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R9HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w9, aes(R9HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead9}
HRS_ds_ahead_w9 <- tibble(id=HRS_ahead_w9$id)
HRS_ds_ahead_w9$height <- car::recode(HRS_ahead_w9$R9HEIGHT*100, "NA=999")
HRS_ds_ahead_w9$height <- labelled(HRS_ds_ahead_w9$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead9, echo=F}
vbl <- HRS_ds_ahead_w9$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w9, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead9, echo=F}
kable(HRS_ahead_w9[which(HRS_ahead_w9$R9HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_ahead9, echo=F}






```







### Wave 10 

#### Study-specific variable description



| **Name** | `R10HEIGHT`|
|-|-|
| **Label** | `W10 Height in meters`|
| **Table name** |`HRS-AHEAD-w10`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead10, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w10','HRS.HRS-AHEAD-w10',
                         variables=list('R10HEIGHT'), missings = TRUE)
HRS_ahead_w10 <- opal.execute(o,'HRS_ahead_w10')
```

```{r local_ahead10, echo=F}

vbl <- HRS_ahead_w10$R10HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R10HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w10, aes(R10HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead10}
HRS_ds_ahead_w10 <- tibble(id=HRS_ahead_w10$id)
HRS_ds_ahead_w10$height <- car::recode(HRS_ahead_w10$R10HEIGHT*100, "NA=999")
HRS_ds_ahead_w10$height <- labelled(HRS_ds_ahead_w10$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead10, echo=F}
vbl <- HRS_ds_ahead_w10$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w10, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation

```{r crosstabulation_ahead10, echo=F}
kable(HRS_ahead_w10[which(HRS_ahead_w10$R10HEIGHT<1),], caption = 'There is an unusual value that is lower than 1m:')
```





```{r importOPAL_ahead10, echo=F}






```







### Wave 11 

#### Study-specific variable description



| **Name** | `R11HEIGHT`|
|-|-|
| **Label** | `W11 Height in meters`|
| **Table name** |`HRS-AHEAD-w11`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ahead11, echo=F}
opal.assign.table.tibble(o, 'HRS_ahead_w11','HRS.HRS-AHEAD-w11',
                         variables=list('R11HEIGHT'), missings = TRUE)
HRS_ahead_w11 <- opal.execute(o,'HRS_ahead_w11')
```

```{r local_ahead11, echo=F}

vbl <- HRS_ahead_w11$R11HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R11HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ahead_w11, aes(R11HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ahead11}
HRS_ds_ahead_w11 <- tibble(id=HRS_ahead_w11$id)
HRS_ds_ahead_w11$height <- car::recode(HRS_ahead_w11$R11HEIGHT*100, "NA=999")
HRS_ds_ahead_w11$height <- labelled(HRS_ds_ahead_w11$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ahead11, echo=F}
vbl <- HRS_ds_ahead_w11$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ahead_w11, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_ahead11, echo=F}

```





```{r importOPAL_ahead11, echo=F}






```




## CODA


### Wave 4 

#### Study-specific variable description



| **Name** | `R4HEIGHT`|
|-|-|
| **Label** | `W4 Height in meters`|
| **Table name** |`HRS-CODA-w4`|
| **Units** |`m`|
| **Description** |  |


```{r assign_coda4, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w4','HRS.HRS-CODA-w4',
                         variables=list('R4HEIGHT'), missings = TRUE)
HRS_coda_w4 <- opal.execute(o,'HRS_coda_w4')
```

```{r local_coda4, echo=F}

vbl <- HRS_coda_w4$R4HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R4HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w4, aes(R4HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda4}
HRS_ds_coda_w4 <- tibble(id=HRS_coda_w4$id)
HRS_ds_coda_w4$height <- car::recode(HRS_coda_w4$R4HEIGHT*100, "NA=999")
HRS_ds_coda_w4$height <- labelled(HRS_ds_coda_w4$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda4, echo=F}
vbl <- HRS_ds_coda_w4$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w4, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda4, echo=F}

```





```{r importOPAL_coda4, echo=F}






```








### Wave 5 

#### Study-specific variable description



| **Name** | `R5HEIGHT`|
|-|-|
| **Label** | `W5 Height in meters`|
| **Table name** |`HRS-CODA-w5`|
| **Units** |`m`|
| **Description** |  |


```{r assign_coda5, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w5','HRS.HRS-CODA-w5',
                         variables=list('R5HEIGHT'), missings = TRUE)
HRS_coda_w5 <- opal.execute(o,'HRS_coda_w5')
```

```{r local_coda5, echo=F}

vbl <- HRS_coda_w5$R5HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R5HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w5, aes(R5HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda5}
HRS_ds_coda_w5 <- tibble(id=HRS_coda_w5$id)
HRS_ds_coda_w5$height <- car::recode(HRS_coda_w5$R5HEIGHT*100, "NA=999")
HRS_ds_coda_w5$height <- labelled(HRS_ds_coda_w5$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda5, echo=F}
vbl <- HRS_ds_coda_w5$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w5, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda5, echo=F}

```





```{r importOPAL_coda5, echo=F}






```








### Wave 6 

#### Study-specific variable description



| **Name** | `R6HEIGHT`|
|-|-|
| **Label** | `W6 Height in meters`|
| **Table name** |`HRS-CODA-w6`|
| **Units** |`m`|
| **Description** |  |

```{r assign_coda6, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w6','HRS.HRS-CODA-w6',
                         variables=list('R6HEIGHT'), missings = TRUE)
HRS_coda_w6 <- opal.execute(o,'HRS_coda_w6')
```

```{r local_coda6, echo=F}

vbl <- HRS_coda_w6$R6HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R6HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w6, aes(R6HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda6}
HRS_ds_coda_w6 <- tibble(id=HRS_coda_w6$id)
HRS_ds_coda_w6$height <- car::recode(HRS_coda_w6$R6HEIGHT*100, "NA=999")
HRS_ds_coda_w6$height <- labelled(HRS_ds_coda_w6$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda6, echo=F}
vbl <- HRS_ds_coda_w6$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w6, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda6, echo=F}

```





```{r importOPAL_coda6, echo=F}






```






### Wave 7 

#### Study-specific variable description



| **Name** | `R7HEIGHT`|
|-|-|
| **Label** | `W7 Height in meters`|
| **Table name** |`HRS-CODA-w7`|
| **Units** |`m`|
| **Description** |  |

```{r assign_coda7, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w7','HRS.HRS-CODA-w7',
                         variables=list('R7HEIGHT'), missings = TRUE)
HRS_coda_w7 <- opal.execute(o,'HRS_coda_w7')
```

```{r local_coda7, echo=F}

vbl <- HRS_coda_w7$R7HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R7HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w7, aes(R7HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda7}
HRS_ds_coda_w7 <- tibble(id=HRS_coda_w7$id)
HRS_ds_coda_w7$height <- car::recode(HRS_coda_w7$R7HEIGHT*100, "NA=999")
HRS_ds_coda_w7$height <- labelled(HRS_ds_coda_w7$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda7, echo=F}
vbl <- HRS_ds_coda_w7$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w7, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda7, echo=F}

```





```{r importOPAL_coda7, echo=F}






```







### Wave 8 

#### Study-specific variable description



| **Name** | `R8HEIGHT`|
|-|-|
| **Label** | `W8 Height in meters`|
| **Table name** |`HRS-CODA-w8`|
| **Units** |`m`|
| **Description** |  |

```{r assign_coda8, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w8','HRS.HRS-CODA-w8',
                         variables=list('R8HEIGHT'), missings = TRUE)
HRS_coda_w8 <- opal.execute(o,'HRS_coda_w8')
```

```{r local_coda8, echo=F}

vbl <- HRS_coda_w8$R8HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R8HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w8, aes(R8HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda8}
HRS_ds_coda_w8 <- tibble(id=HRS_coda_w8$id)
HRS_ds_coda_w8$height <- car::recode(HRS_coda_w8$R8HEIGHT*100, "NA=999")
HRS_ds_coda_w8$height <- labelled(HRS_ds_coda_w8$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda8, echo=F}
vbl <- HRS_ds_coda_w8$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w8, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda8, echo=F}

```





```{r importOPAL_coda8, echo=F}






```







### Wave 9 

#### Study-specific variable description



| **Name** | `R9HEIGHT`|
|-|-|
| **Label** | `W9 Height in meters`|
| **Table name** |`HRS-CODA-w9`|
| **Units** |`m`|
| **Description** |  |

```{r assign_coda9, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w9','HRS.HRS-CODA-w9',
                         variables=list('R9HEIGHT'), missings = TRUE)
HRS_coda_w9 <- opal.execute(o,'HRS_coda_w9')
```

```{r local_coda9, echo=F}

vbl <- HRS_coda_w9$R9HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R9HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w9, aes(R9HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda9}
HRS_ds_coda_w9 <- tibble(id=HRS_coda_w9$id)
HRS_ds_coda_w9$height <- car::recode(HRS_coda_w9$R9HEIGHT*100, "NA=999")
HRS_ds_coda_w9$height <- labelled(HRS_ds_coda_w9$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda9, echo=F}
vbl <- HRS_ds_coda_w9$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w9, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda9, echo=F}

```





```{r importOPAL_coda9, echo=F}






```







### Wave 10 

#### Study-specific variable description



| **Name** | `R10HEIGHT`|
|-|-|
| **Label** | `W10 Height in meters`|
| **Table name** |`HRS-CODA-w10`|
| **Units** |`m`|
| **Description** |  |

```{r assign_coda10, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w10','HRS.HRS-CODA-w10',
                         variables=list('R10HEIGHT'), missings = TRUE)
HRS_coda_w10 <- opal.execute(o,'HRS_coda_w10')
```

```{r local_coda10, echo=F}

vbl <- HRS_coda_w10$R10HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R10HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w10, aes(R10HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda10}
HRS_ds_coda_w10 <- tibble(id=HRS_coda_w10$id)
HRS_ds_coda_w10$height <- car::recode(HRS_coda_w10$R10HEIGHT*100, "NA=999")
HRS_ds_coda_w10$height <- labelled(HRS_ds_coda_w10$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda10, echo=F}
vbl <- HRS_ds_coda_w10$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w10, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda10, echo=F}

```





```{r importOPAL_coda10, echo=F}






```







### Wave 11 

#### Study-specific variable description



| **Name** | `R11HEIGHT`|
|-|-|
| **Label** | `W11 Height in meters`|
| **Table name** |`HRS-CODA-w11`|
| **Units** |`m`|
| **Description** |  |

```{r assign_coda11, echo=F}
opal.assign.table.tibble(o, 'HRS_coda_w11','HRS.HRS-CODA-w11',
                         variables=list('R11HEIGHT'), missings = TRUE)
HRS_coda_w11 <- opal.execute(o,'HRS_coda_w11')
```

```{r local_coda11, echo=F}

vbl <- HRS_coda_w11$R11HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R11HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_coda_w11, aes(R11HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_coda11}
HRS_ds_coda_w11 <- tibble(id=HRS_coda_w11$id)
HRS_ds_coda_w11$height <- car::recode(HRS_coda_w11$R11HEIGHT*100, "NA=999")
HRS_ds_coda_w11$height <- labelled(HRS_ds_coda_w11$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_coda11, echo=F}
vbl <- HRS_ds_coda_w11$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_coda_w11, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_coda11, echo=F}

```





```{r importOPAL_coda11, echo=F}






```





## The War Baby (WB)

### Wave 4  

#### Study-specific variable description



| **Name** | `R4HEIGHT`|
|-|-|
| **Label** | `W4 Height in meters`|
| **Table name** |`HRS-WB-w4`|
| **Units** |`m`|
| **Description** |  |


```{r assign_wb4, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w4','HRS.HRS-WB-w4',
                         variables=list('R4HEIGHT'), missings = TRUE)
HRS_wb_w4 <- opal.execute(o,'HRS_wb_w4')
```

```{r local_wb4, echo=F}

vbl <- HRS_wb_w4$R4HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R4HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w4, aes(R4HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb4}
HRS_ds_wb_w4 <- tibble(id=HRS_wb_w4$id)
HRS_ds_wb_w4$height <- car::recode(HRS_wb_w4$R4HEIGHT*100, "NA=999")
HRS_ds_wb_w4$height <- labelled(HRS_ds_wb_w4$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb4, echo=F}
vbl <- HRS_ds_wb_w4$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w4, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb4, echo=F}

```





```{r importOPAL_wb4, echo=F}






```









### Wave 5  

#### Study-specific variable description



| **Name** | `R5HEIGHT`|
|-|-|
| **Label** | `W5 Height in meters`|
| **Table name** |`HRS-WB-w5`|
| **Units** |`m`|
| **Description** |  |

```{r assign_wb5, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w5','HRS.HRS-WB-w5',
                         variables=list('R5HEIGHT'), missings = TRUE)
HRS_wb_w5 <- opal.execute(o,'HRS_wb_w5')
```

```{r local_wb5, echo=F}

vbl <- HRS_wb_w5$R5HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R5HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w5, aes(R5HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb5}
HRS_ds_wb_w5 <- tibble(id=HRS_wb_w5$id)
HRS_ds_wb_w5$height <- car::recode(HRS_wb_w5$R5HEIGHT*100, "NA=999")
HRS_ds_wb_w5$height <- labelled(HRS_ds_wb_w5$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb5, echo=F}
vbl <- HRS_ds_wb_w5$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w5, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb5, echo=F}

```





```{r importOPAL_wb5, echo=F}






```








### Wave 6  

#### Study-specific variable description



| **Name** | `R6HEIGHT`|
|-|-|
| **Label** | `W6 Height in meters`|
| **Table name** |`HRS-WB-w6`|
| **Units** |`m`|
| **Description** |  |

```{r assign_wb6, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w6','HRS.HRS-WB-w6',
                         variables=list('R6HEIGHT'), missings = TRUE)
HRS_wb_w6 <- opal.execute(o,'HRS_wb_w6')
```

```{r local_wb6, echo=F}

vbl <- HRS_wb_w6$R6HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R6HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w6, aes(R6HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb6}
HRS_ds_wb_w6 <- tibble(id=HRS_wb_w6$id)
HRS_ds_wb_w6$height <- car::recode(HRS_wb_w6$R6HEIGHT*100, "NA=999")
HRS_ds_wb_w6$height <- labelled(HRS_ds_wb_w6$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb6, echo=F}
vbl <- HRS_ds_wb_w6$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w6, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb6, echo=F}

```





```{r importOPAL_wb6, echo=F}






```






### Wave 7  

#### Study-specific variable description



| **Name** | `R7HEIGHT`|
|-|-|
| **Label** | `W7 Height in meters`|
| **Table name** |`HRS-WB-w7`|
| **Units** |`m`|
| **Description** |  |


```{r assign_wb7, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w7','HRS.HRS-WB-w7',
                         variables=list('R7HEIGHT'), missings = TRUE)
HRS_wb_w7 <- opal.execute(o,'HRS_wb_w7')
```

```{r local_wb7, echo=F}

vbl <- HRS_wb_w7$R7HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R7HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w7, aes(R7HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb7}
HRS_ds_wb_w7 <- tibble(id=HRS_wb_w7$id)
HRS_ds_wb_w7$height <- car::recode(HRS_wb_w7$R7HEIGHT*100, "NA=999")
HRS_ds_wb_w7$height <- labelled(HRS_ds_wb_w7$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb7, echo=F}
vbl <- HRS_ds_wb_w7$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w7, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb7, echo=F}

```





```{r importOPAL_wb7, echo=F}






```








### Wave 8  

#### Study-specific variable description



| **Name** | `R8HEIGHT`|
|-|-|
| **Label** | `W8 Height in meters`|
| **Table name** |`HRS-WB-w8`|
| **Units** |`m`|
| **Description** |  |

```{r assign_wb8, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w8','HRS.HRS-WB-w8',
                         variables=list('R8HEIGHT'), missings = TRUE)
HRS_wb_w8 <- opal.execute(o,'HRS_wb_w8')
```

```{r local_wb8, echo=F}

vbl <- HRS_wb_w8$R8HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R8HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w8, aes(R8HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb8}
HRS_ds_wb_w8 <- tibble(id=HRS_wb_w8$id)
HRS_ds_wb_w8$height <- car::recode(HRS_wb_w8$R8HEIGHT*100, "NA=999")
HRS_ds_wb_w8$height <- labelled(HRS_ds_wb_w8$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb8, echo=F}
vbl <- HRS_ds_wb_w8$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w8, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb8, echo=F}

```





```{r importOPAL_wb8, echo=F}






```








### Wave 9  

#### Study-specific variable description



| **Name** | `R9HEIGHT`|
|-|-|
| **Label** | `W9 Height in meters`|
| **Table name** |`HRS-WB-w9`|
| **Units** |`m`|
| **Description** |  |


```{r assign_wb9, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w9','HRS.HRS-WB-w9',
                         variables=list('R9HEIGHT'), missings = TRUE)
HRS_wb_w9 <- opal.execute(o,'HRS_wb_w9')
```

```{r local_wb9, echo=F}

vbl <- HRS_wb_w9$R9HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R9HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w9, aes(R9HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb9}
HRS_ds_wb_w9 <- tibble(id=HRS_wb_w9$id)
HRS_ds_wb_w9$height <- car::recode(HRS_wb_w9$R9HEIGHT*100, "NA=999")
HRS_ds_wb_w9$height <- labelled(HRS_ds_wb_w9$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb9, echo=F}
vbl <- HRS_ds_wb_w9$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w9, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb9, echo=F}

```





```{r importOPAL_wb9, echo=F}






```







### Wave 10  

#### Study-specific variable description



| **Name** | `R10HEIGHT`|
|-|-|
| **Label** | `W10 Height in meters`|
| **Table name** |`HRS-WB-w10`|
| **Units** |`m`|
| **Description** |  |

```{r assign_wb10, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w10','HRS.HRS-WB-w10',
                         variables=list('R10HEIGHT'), missings = TRUE)
HRS_wb_w10 <- opal.execute(o,'HRS_wb_w10')
```

```{r local_wb10, echo=F}

vbl <- HRS_wb_w10$R10HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R10HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w10, aes(R10HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb10}
HRS_ds_wb_w10 <- tibble(id=HRS_wb_w10$id)
HRS_ds_wb_w10$height <- car::recode(HRS_wb_w10$R10HEIGHT*100, "NA=999")
HRS_ds_wb_w10$height <- labelled(HRS_ds_wb_w10$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb10, echo=F}
vbl <- HRS_ds_wb_w10$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w10, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb10, echo=F}

```





```{r importOPAL_wb10, echo=F}






```







### Wave 11  

#### Study-specific variable description



| **Name** | `R11HEIGHT`|
|-|-|
| **Label** | `W11 Height in meters`|
| **Table name** |`HRS-WB-w11`|
| **Units** |`m`|
| **Description** |  |


```{r assign_wb11, echo=F}
opal.assign.table.tibble(o, 'HRS_wb_w11','HRS.HRS-WB-w11',
                         variables=list('R11HEIGHT'), missings = TRUE)
HRS_wb_w11 <- opal.execute(o,'HRS_wb_w11')
```

```{r local_wb11, echo=F}

vbl <- HRS_wb_w11$R11HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R11HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_wb_w11, aes(R11HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_wb11}
HRS_ds_wb_w11 <- tibble(id=HRS_wb_w11$id)
HRS_ds_wb_w11$height <- car::recode(HRS_wb_w11$R11HEIGHT*100, "NA=999")
HRS_ds_wb_w11$height <- labelled(HRS_ds_wb_w11$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_wb11, echo=F}
vbl <- HRS_ds_wb_w11$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_wb_w11, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_wb11, echo=F}

```





```{r importOPAL_wb11, echo=F}






```




## The Early Baby Boomer (EBB)


### Wave 7  

#### Study-specific variable description



| **Name** | `R7HEIGHT`|
|-|-|
| **Label** | `W7 Height in meters`|
| **Table name** |`HRS-EBB-w7`|
| **Units** |`m`|
| **Description** |  |



```{r assign_ebb7, echo=F}
opal.assign.table.tibble(o, 'HRS_ebb_w7','HRS.HRS-EBB-w7',
                         variables=list('R7HEIGHT'), missings = TRUE)
HRS_ebb_w7 <- opal.execute(o,'HRS_ebb_w7')
```

```{r local_ebb7, echo=F}

vbl <- HRS_ebb_w7$R7HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R7HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ebb_w7, aes(R7HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ebb7}
HRS_ds_ebb_w7 <- tibble(id=HRS_ebb_w7$id)
HRS_ds_ebb_w7$height <- car::recode(HRS_ebb_w7$R7HEIGHT*100, "NA=999")
HRS_ds_ebb_w7$height <- labelled(HRS_ds_ebb_w7$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ebb7, echo=F}
vbl <- HRS_ds_ebb_w7$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ebb_w7, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_ebb7, echo=F}

```





```{r importOPAL_ebb7, echo=F}






```







### Wave 8  

#### Study-specific variable description



| **Name** | `R8HEIGHT`|
|-|-|
| **Label** | `W8 Height in meters`|
| **Table name** |`HRS-EBB-w8`|
| **Units** |`m`|
| **Description** |  |

```{r assign_ebb8, echo=F}
opal.assign.table.tibble(o, 'HRS_ebb_w8','HRS.HRS-EBB-w8',
                         variables=list('R8HEIGHT'), missings = TRUE)
HRS_ebb_w8 <- opal.execute(o,'HRS_ebb_w8')
```

```{r local_ebb8, echo=F}

vbl <- HRS_ebb_w8$R8HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R8HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ebb_w8, aes(R8HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ebb8}
HRS_ds_ebb_w8 <- tibble(id=HRS_ebb_w8$id)
HRS_ds_ebb_w8$height <- car::recode(HRS_ebb_w8$R8HEIGHT*100, "NA=999")
HRS_ds_ebb_w8$height <- labelled(HRS_ds_ebb_w8$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ebb8, echo=F}
vbl <- HRS_ds_ebb_w8$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ebb_w8, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_ebb8, echo=F}

```





```{r importOPAL_ebb8, echo=F}






```







### Wave 9  

#### Study-specific variable description



| **Name** | `R9HEIGHT`|
|-|-|
| **Label** | `W9 Height in meters`|
| **Table name** |`HRS-EBB-w9`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ebb9, echo=F}
opal.assign.table.tibble(o, 'HRS_ebb_w9','HRS.HRS-EBB-w9',
                         variables=list('R9HEIGHT'), missings = TRUE)
HRS_ebb_w9 <- opal.execute(o,'HRS_ebb_w9')
```

```{r local_ebb9, echo=F}

vbl <- HRS_ebb_w9$R9HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R9HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ebb_w9, aes(R9HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ebb9}
HRS_ds_ebb_w9 <- tibble(id=HRS_ebb_w9$id)
HRS_ds_ebb_w9$height <- car::recode(HRS_ebb_w9$R9HEIGHT*100, "NA=999")
HRS_ds_ebb_w9$height <- labelled(HRS_ds_ebb_w9$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ebb9, echo=F}
vbl <- HRS_ds_ebb_w9$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ebb_w9, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_ebb9, echo=F}

```





```{r importOPAL1, echo=F}






```







### Wave 10  

#### Study-specific variable description



| **Name** | `R10HEIGHT`|
|-|-|
| **Label** | `W10 Height in meters`|
| **Table name** |`HRS-EBB-w10`|
| **Units** |`m`|
| **Description** |  |

```{r assign_ebb10, echo=F}
opal.assign.table.tibble(o, 'HRS_ebb_w10','HRS.HRS-EBB-w10',
                         variables=list('R10HEIGHT'), missings = TRUE)
HRS_ebb_w10 <- opal.execute(o,'HRS_ebb_w10')
```

```{r local_ebb10, echo=F}

vbl <- HRS_ebb_w10$R10HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R10HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ebb_w10, aes(R10HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ebb10}
HRS_ds_ebb_w10 <- tibble(id=HRS_ebb_w10$id)
HRS_ds_ebb_w10$height <- car::recode(HRS_ebb_w10$R10HEIGHT*100, "NA=999")
HRS_ds_ebb_w10$height <- labelled(HRS_ds_ebb_w10$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ebb10, echo=F}
vbl <- HRS_ds_ebb_w10$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ebb_w10, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_ebb10, echo=F}

```





```{r importOPAL_ebb10, echo=F}






```








### Wave 11  

#### Study-specific variable description



| **Name** | `R11HEIGHT`|
|-|-|
| **Label** | `W11 Height in meters`|
| **Table name** |`HRS-EBB-w11`|
| **Units** |`m`|
| **Description** |  |


```{r assign_ebb11, echo=F}
opal.assign.table.tibble(o, 'HRS_ebb_w11','HRS.HRS-EBB-w11',
                         variables=list('R11HEIGHT'), missings = TRUE)
HRS_ebb_w11 <- opal.execute(o,'HRS_ebb_w11')
```

```{r local_ebb11, echo=F}

vbl <- HRS_ebb_w11$R11HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R11HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_ebb_w11, aes(R11HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_ebb11}
HRS_ds_ebb_w11 <- tibble(id=HRS_ebb_w11$id)
HRS_ds_ebb_w11$height <- car::recode(HRS_ebb_w11$R11HEIGHT*100, "NA=999")
HRS_ds_ebb_w11$height <- labelled(HRS_ds_ebb_w11$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_ebb11, echo=F}
vbl <- HRS_ds_ebb_w11$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_ebb_w11, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_ebb11, echo=F}

```





```{r importOPAL_ebb11, echo=F}






```



## The Mid Baby Boomer (MBB)


### Wave 10  

#### Study-specific variable description



| **Name** | `R10HEIGHT`|
|-|-|
| **Label** | `W10 Height in meters`|
| **Table name** |`HRS-MBB-w10`|
| **Units** |`m`|
| **Description** |  |

```{r assign_mbb10, echo=F}
opal.assign.table.tibble(o, 'HRS_mbb_w10','HRS.HRS-MBB-w10',
                         variables=list('R10HEIGHT'), missings = TRUE)
HRS_mbb_w10 <- opal.execute(o,'HRS_mbb_w10')
```

```{r local_mbb10, echo=F}

vbl <- HRS_mbb_w10$R10HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R10HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_mbb_w10, aes(R10HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`

**R script:**

```{r harmo_mbb10}
HRS_ds_mbb_w10 <- tibble(id=HRS_mbb_w10$id)
HRS_ds_mbb_w10$height <- car::recode(HRS_mbb_w10$R10HEIGHT*100, "NA=999")
HRS_ds_mbb_w10$height <- labelled(HRS_ds_mbb_w10$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_mbb10, echo=F}
vbl <- HRS_ds_mbb_w10$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_mbb_w10, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_mbb10, echo=F}

```





```{r importOPAL_mbb10, echo=F}






```






### Wave 11  

#### Study-specific variable description



| **Name** | `R11HEIGHT`|
|-|-|
| **Label** | `W11 Height in meters`|
| **Table name** |`HRS-MBB-w11`|
| **Units** |`m`|
| **Description** |  |

```{r assign_mbb11, echo=F}
opal.assign.table.tibble(o, 'HRS_mbb_w11','HRS.HRS-MBB-w11',
                         variables=list('R11HEIGHT'), missings = TRUE)
HRS_mbb_w11 <- opal.execute(o,'HRS_mbb_w11')
```

```{r local_mbb11, echo=F}

vbl <- HRS_mbb_w11$R11HEIGHT
kable(Continuous_summary(vbl, missing_values = NA)[3], caption = "R11HEIGHT")
pander(Continuous_summary(vbl, missing_values = NA)$summary, caption = "Summary")
ggplot(HRS_mbb_w11, aes(R11HEIGHT)) + geom_histogram(stat="count", fill="steelblue") + xlab("Height") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same as the study-specific variable converted into cms, therefore the study-specific variable has to be multiplied by 100 and the missing values have to be recoded as follows:

* `NA as 999`


**R script:**

```{r harmo_mbb11}
HRS_ds_mbb_w11 <- tibble(id=HRS_mbb_w11$id)
HRS_ds_mbb_w11$height <- car::recode(HRS_mbb_w11$R11HEIGHT*100, "NA=999")
HRS_ds_mbb_w11$height <- labelled(HRS_ds_mbb_w11$height, labels = c("Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996, "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```


#### Statistics of the new harmonised variable
```{r descript_mbb11, echo=F}
vbl <- HRS_ds_mbb_w11$height
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(HRS_ds_mbb_w11, aes(height)) + geom_histogram(stat="count", fill="steelblue", data = .%>% filter(!vbl %in% miss_values_vector)) + xlab("height") + ylab("Frequency") 
```

#### Validation
```{r crosstabulation_mbb11, echo=F}

```





```{r importOPAL_mbb11, echo=F}






```






## Summary of descriptive statistics of the harmonised variable accross populations and waves

```{r helpfunctions, echo=F}

labelling <- function(l.hds,m.hds){
  
  # Labelling of the tibbles with continuous data and creating new tibbles with all missings recodified as NA

  for(name in names(l.hds)) {
    # In the aux_object we copy the old tibble to recodify all missing values as NA.
    aux_object <- l.hds[[name]]
    # Labelling of variables
    label(l.hds[[name]][[2]]) <- label(aux_object[[2]]) <- ds_label
    # Labelling of categories (for continues variables, only missing values)
    l.hds[[name]][[2]] <- labelled(l.hds[[name]][[2]], labels = cont_label)
    aux_object[[2]] <- car::recode(aux_object[[2]], "miss_values_vector=NA")
    # Saving the recodified tibble in list m.hds
    m.hds[[name]] <- aux_object
    rm(aux_object)
  }
  return(list(l.hds,m.hds))

}

# Creation of summary tables for continuous data

summaries <- function(l.hds,m.hds,lnames){

  # Creation of column with summary table categories
  t.summ <- summary(m.hds[[1]][2])[1:6]
  # Adding of missing/no-missing values categories
  t.hds <- c(substr(t.summ,1,regexpr(":", t.summ, fixed=T)-1),labels(Continuous_summary(l.hds[[1]][[2]],missing_values = miss_values_vector)$values_table)[[2]])
  # For each wave/population in l.hds, add the correponding values
  for (i in seq_along(l.hds)){
    # First, summary values
    t.summ <- summary(m.hds[[i]][2])[1:6]
    # Next, missing/no-missing values
    t.hds <- cbind(t.hds,c(as.numeric(substr(t.summ,regexpr(":", t.summ, fixed=T)+1,nchar(t.summ))),as.numeric(Continuous_summary(l.hds[[i]][[2]],missing_values = miss_values_vector)$values_table[1,])))
  }
  # Add sample size for each wave/population
  t.hds <- rbind(t.hds,c("sample size", sapply(l.hds,function(wave) length(wave[[1]]))))
  # Add wave/population names
  dimnames(t.hds)[[2]] <- c(dimnames(summary(m.hds[[1]][2]))[[2]],lnames)
  return(t.hds)
  
}

# Save data tables
# 
savingRD <- function(l.hds,vbl_name){

  for(index in seq_along(l.hds)){
    assign(vbl_name,l.hds[[index]])
    save(vbl_name,list = vbl_name, file = paste0(datafolder,names(l.hds)[index],"/",vbl_name,".RData"))
  }

}

```




```{r summ, echo=F}

# All study waves and populations with abbreviated and descriptive names


hrw.hrw.cw <- list(hrs_w1 = c("hrs_w1","HRS w1"), hrs_w2 = c("hrs_w2","HRS w2"), hrs_w3 = c("hrs_w3","HRS w3"), hrs_w4 = c("hrs_w4","HRS w4"), hrs_w5 = c("hrs_w5","HRS w5"), hrs_w6 = c("hrs_w6","HRS w6"), hrs_w7 = c("hrs_w7","HRS w7"), hrs_w8 = c("hrs_w8","HRS w8"), hrs_w9 = c("hrs_w9","HRS w9"), hrs_w10 = c("hrs_w10","HRS w10"), hrs_w11 = c("hrs_w11","HRS w11"))

hrw.ahead.cw <- list(ahead_w2 = c("ahead_w2","AHEAD w2"), ahead_w3 = c("ahead_w3","AHEAD w3"), ahead_w4 = c("ahead_w4","AHEAD w4"), ahead_w5 = c("ahead_w5","AHEAD w5"), ahead_w6 = c("ahead_w6","AHEAD w6"), ahead_w7 = c("ahead_w7","AHEAD w7"), ahead_w8 = c("ahead_w8","AHEAD w8"), ahead_w9 = c("ahead_w9","AHEAD w9"), ahead_w10 = c("ahead_w10","AHEAD w10"), ahead_w11 = c("ahead_w11","AHEAD w11"))

hrw.coda.cw <- list(coda_w4 = c("coda_w4","CODA w4"), coda_w5 = c("coda_w5","CODA w5"), coda_w6 = c("coda_w6","CODA w6"), coda_w7 = c("coda_w7","CODA w7"), coda_w8 = c("coda_w8","CODA w8"), coda_w9 = c("coda_w9","CODA w9"), coda_w10 = c("coda_w10","CODA w10"), coda_w11 = c("coda_w11","CODA w11"))

hrw.wb.cw <- list(wb_w4 = c("wb_w4","WB w4"), wb_w5 = c("wb_w5","WB w5"), wb_w6 = c("wb_w6","WB w6"), wb_w7 = c("wb_w7","WB w7"), wb_w8 = c("wb_w8","WB w8"), wb_w9 = c("wb_w9","WB w9"), wb_w10 = c("wb_w10","WB w10"), wb_w11 = c("wb_w11","WB w11"))

hrw.ebb.cw <- list(ebb_w7 = c("ebb_w7","EBB w7"), ebb_w8 = c("ebb_w8","EBB w8"), ebb_w9 = c("ebb_w9","EBB w9"), ebb_w10 = c("ebb_w10","EBB w10"), ebb_w11 = c("ebb_w11","EBB w11"))

hrw.mbb.cw <- list(mbb_w10 = c("mbb_w10","MBB w10"), mbb_w11 = c("mbb_w11","MBB w11"))



# HRS

# Consider only harmonised waves
l.hds <- lapply(hrw.hrw.cw, function(wname) if(exists(paste0("HRS_ds_",wname[1]))){wname = list(get(paste0("HRS_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)

if(length(l.hds)>0){
  # Labelling tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]])
  # Printing summaries
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]])),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}






# AHEAD

# Consider only harmonised waves
l.hds <- lapply(hrw.ahead.cw, function(wname) if(exists(paste0("HRS_ds_",wname[1]))){wname = list(get(paste0("HRS_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)


if(length(l.hds)>0){
  # Labelling tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]])
  # Printing summaries
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]])),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}








# CODA

# Consider only harmonised waves
l.hds <- lapply(hrw.coda.cw, function(wname) if(exists(paste0("HRS_ds_",wname[1]))){wname = list(get(paste0("HRS_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)

if(length(l.hds)>0){
  # Labelling tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]])
  # Printing table of categories
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]])),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}





# WB

# Consider only harmonised waves
l.hds <- lapply(hrw.wb.cw, function(wname) if(exists(paste0("HRS_ds_",wname[1]))){wname = list(get(paste0("HRS_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)

if(length(l.hds)>0){
  # Labelling tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]])
  # Printing table of categories
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]])),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}





# EBB

# Consider only harmonised waves
l.hds <- lapply(hrw.ebb.cw, function(wname) if(exists(paste0("HRS_ds_",wname[1]))){wname = list(get(paste0("HRS_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)

if(length(l.hds)>0){
  # Labelling tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]])
  # Printing table of categories
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]])),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}




# MBB

# Consider only harmonised waves
l.hds <- lapply(hrw.mbb.cw, function(wname) if(exists(paste0("HRS_ds_",wname[1]))){wname = list(get(paste0("HRS_ds_",wname[1])),wname[2])})
nullw <- which(sapply(l.hds, is.null))

if(length(nullw)!=0){
  l.hds <- l.hds[-nullw]
}

m.hds <- list()

lmlist <- list(lapply(l.hds,function(x)x[[1]]),m.hds)

if(length(l.hds)>0){
  # Labelling tibbles
  lmlist <- labelling(lmlist[[1]],lmlist[[2]])
  # Printing table of categories
  print(knitr::kable(summaries(lmlist[[1]],lmlist[[2]],lapply(l.hds,function(x)x[[2]])),caption=ds_label))
  # Saving tibbles
  savingRD(lmlist[[1]],vbl_name=hd_vbl)
}




```






# Quality estimation
Units were converted for this variable.



```{r closeRsession, echo=FALSE}
opal.logout(o)
```
