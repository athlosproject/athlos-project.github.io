---
title: "Retired"
output:
  html_document:
    highlight: tango
    number_sections: yes
    theme: united
    toc: yes
    toc_depth: 6
    toc_float:
      collapsed: yes
      smooth_scroll: yes
---

<!-- ########################################################## --> 
<!-- # Installation, loading packages -->
```{r setup, include=FALSE}
source("../../setup_iago.r")
```
<!-- ########################################################## --> 
<!-- # Loading DS description -->
```{r main, child='../../_DS_Rmd/retiredDS.Rmd'}
```
```{r global, echo=F}
source('../../_DS_Rmd/retiredDS.R')
```


# Data process



## Australia


### Wave 1


#### Study-specific variable description

| **Name** | `RETIRED`|`WHATYEAR`|`ybirth`|
|-|-|-|-|
| **Label** | `Are you retired from your last job?`|`In what year did you retire?`|`Year of birth`|
| **Table name**  | `ALSA-Wave1` |`ALSA-Wave1` |`ybirth`|
| **Categories** | `1 = Yes`<br/>`2 = No` |`continuous`|`continuous`|
| **Missings** | `9`<br/>`NA` |`9999`<br/>`NA`|  |
| **Description** |  |  |  |

```{r assign w1, echo=F}

opal.assign.table.tibble(o, 'ALSA_w1','ALSA.ALSA-Wave1',variables=list('RETIRED','WHATYEAR'), missings = T)
ALSA_w1 <- opal.execute(o,'ALSA_w1')
load(paste0(datafolder,"w1/ybirth.RData"))
ALSA_w1$ybirth <- ybirth$ybirth
rm(ybirth)

```

```{r local w1, echo=F}

vbl <- ALSA_w1$RETIRED
kable(Categorical_summary(vbl, missing_values = c(9))[3], caption = 'RETIRED')
kable(Categorical_summary(vbl, missing_values = c(9))[1], caption = "Category")
ggplot(ALSA_w1, aes(x=factor(RETIRED))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

vbl <- ALSA_w1$WHATYEAR
kable(Continuous_summary(var = vbl, missing_values = c(9999))[3], caption = 'WHATYEAR')
pander(Continuous_summary(vbl, missing_values = c(9999))$summary, caption = "Summary")
ggplot(filter(ALSA_w1,vbl!=9999), aes(WHATYEAR)) + geom_bar(stat="count", width=0.4, fill="steelblue" ) + xlab(label(vbl)) + ylab("Frequency")

vbl <- ALSA_w1$ybirth
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = 'ybirth')
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(ALSA_w1, aes(ybirth)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")+xlim(1885,1930)

```


#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `change 2 into 0`
* `keep up 1 into 1`
* `9, NA into 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `WHATYEAR == 9999 or NA into 999`
* `otherwise, age_retired = WHATYEAR - ybirth`

**R script:**

```{r harmo w1}

ALSA_ds_w1 <- tibble(id=ALSA_w1$id)
ALSA_ds_w1$retired <- car::recode(ALSA_w1$RETIRED, "2 = 0; c(9,NA) = 999")

ALSA_ds_w1$age_retired <- rep(999,length(ALSA_w1$id))
ALSA_ds_w1$age_retired[which(!is.na(ALSA_w1$WHATYEAR) & ALSA_w1$WHATYEAR != 9999)] <- ALSA_w1$WHATYEAR[which(!is.na(ALSA_w1$WHATYEAR) & ALSA_w1$WHATYEAR != 9999)] - ALSA_w1$ybirth[which(!is.na(ALSA_w1$WHATYEAR) & ALSA_w1$WHATYEAR != 9999)]

# Global table
ALSA_ds <- ALSA_ds_w1

```


#### Statistics of the new harmonized variable

```{r descript w1, echo=F}

vbl <- ALSA_ds_w1$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w1, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w1$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w1,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation

```{r crosstabulation w1, echo=F}

AfterH <- table(ALSA_ds_w1$retired)
X <- table(ALSA_w1$RETIRED,useNA = "ifany")
BeforeH <- c(X[2:1],sum(X[3:4]))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("2->0","1->1","9,NA->999")
kable(C)

```




### Wave 2

#### Study-specific variable description

There is no variable.

```{r assign w2, echo=F}

load(paste0(datafolder,"w2/ybirth.RData"))
ALSA_w2 <- tibble(id=ybirth$id)

```

#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `if retired == 1 previously, retired = 1`
* `otherwise, retired = 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w2}

ALSA_w2 <- left_join(ALSA_w2,ALSA_ds, by="id")
ALSA_ds_w2 <- tibble(id=ALSA_w2$id)
ALSA_ds_w2$retired <- car::recode(ALSA_w2$retired, "c(0,NA)=999")
ALSA_ds_w2$age_retired <- car::recode(ALSA_w2$age_retired, "NA=999")

```


#### Statistics of the new harmonized variable

```{r descript w2, echo=F}

vbl <- ALSA_ds_w2$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w2, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w2$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w2,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation



### Wave 3


#### Study-specific variable description

| **Name** | `CURRWKW3`|
|-|-|
| **Label** | `CURRENTLY WORKING`|
| **Table name**  | `ALSA-Wave3` |
| **Categories** | `1 = Yes`<br/>`2 = No` |
| **Missings** | `9`<br/>`NA` |
| **Description** |  |

```{r assign w3, echo=F}

opal.assign.table.tibble(o, 'ALSA_w3','ALSA.ALSA-Wave3',variables=list('CURRWKW3'), missings = T)
ALSA_w3 <- opal.execute(o,'ALSA_w3')

```

```{r local w3, echo=F}

vbl <- ALSA_w3$CURRWKW3
kable(Categorical_summary(vbl, missing_values = c(9))[3], caption = names(ALSA_w3)[2])
kable(Categorical_summary(vbl, missing_values = c(9))[1], caption = "Category")
ggplot(ALSA_w3, aes(x=factor(CURRWKW3))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `retired w1 != 1 and CURRWKW3 == 1 into 0`
* `retired w1 == 1 into 1`
* `NA into 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w3}

ALSA_w3 <- left_join(ALSA_w3,ALSA_ds, by="id")

ALSA_ds_w3 <- tibble(id=ALSA_w3$id)
ALSA_ds_w3$retired <- rep(999,length(ALSA_w3$id))
ALSA_ds_w3$retired[which(ALSA_w3$retired==1)] <- 1
ALSA_ds_w3$retired[which((ALSA_w3$retired!=1 | is.na(ALSA_w3$retired)) & ALSA_w3$CURRWKW3==1)] <- 0
ALSA_ds_w3$age_retired <- car::recode(ALSA_w3$age_retired, "NA=999")

# Update of global table
assign_merge <- function(endtib,sttib,log_vec=c(T),col_end_index=NULL,col_st_index=NULL){
  newtib <- left_join(endtib,sttib,by="id")
  newtib[log_vec,col_end_index] <- newtib[log_vec,col_st_index]
  id_ind <- match("id",names(newtib))
  newtib <- newtib[,c(id_ind,col_end_index)]
  names(newtib)[2] <- "x"
  newtib$x <- car::recode(newtib$x,"NA=999")
  names(newtib)[2] <- names(endtib)[col_end_index]
  newtib
}
ALSA_ds_prov <- ALSA_ds[,c(1,2)]
ALSA_ds_prov <- assign_merge(ALSA_ds_prov,ALSA_ds_w3,log_vec=(ALSA_ds_prov$id %in% ALSA_ds_w3$id),col_end_index = 2, col_st_index = 3)
ALSA_ds$retired <- ALSA_ds_prov$retired
rm(ALSA_ds_prov)

```


#### Statistics of the new harmonized variable

```{r descript w3, echo=F}

vbl <- ALSA_ds_w3$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w3, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w3$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w3,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation

```{r crosstabulation w3, echo=F}

AfterH <- table(ALSA_ds_w3$retired)
X <- c(
  sum((ALSA_w3$retired!=1 | is.na(ALSA_w3$retired)) & ALSA_w3$CURRWKW3==1,na.rm = T),
  sum(ALSA_w3$retired==1,na.rm = T)
)
BeforeH <- c(X[1:2],length(ALSA_w3$id)-sum(X))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("CURRWKW3->0","retired->1","otherwise->999")
kable(C)

```



### Wave 4

#### Study-specific variable description

There is no variable.

```{r assign w4, echo=F}

load(paste0(datafolder,"w4/ybirth.RData"))
ALSA_w4 <- tibble(id=ybirth$id)

```

#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `if retired == 1 previously, retired = 1`
* `otherwise, retired = 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w4}

ALSA_w4 <- left_join(ALSA_w4,ALSA_ds, by="id")
ALSA_ds_w4 <- tibble(id=ALSA_w4$id)
ALSA_ds_w4$retired <- car::recode(ALSA_w4$retired, "c(0,NA)=999; miss_values_vector=999")
ALSA_ds_w4$age_retired <- car::recode(ALSA_w4$age_retired, "NA=999; miss_values_vector=999")

```


#### Statistics of the new harmonized variable

```{r descript w4, echo=F}

vbl <- ALSA_ds_w4$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w4, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w4$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w4,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation





### Wave 5

#### Study-specific variable description

There is no variable.

```{r assign w5, echo=F}

load(paste0(datafolder,"w5/ybirth.RData"))
ALSA_w5 <- tibble(id=ybirth$id)

```

#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `if retired == 1 previously, retired = 1`
* `otherwise, retired = 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w5}

ALSA_w5 <- left_join(ALSA_w5,ALSA_ds, by="id")
ALSA_ds_w5 <- tibble(id=ALSA_w5$id)
ALSA_ds_w5$retired <- car::recode(ALSA_w5$retired, "c(0,NA)=999; miss_values_vector=999")
ALSA_ds_w5$age_retired <- car::recode(ALSA_w5$age_retired, "NA=999; miss_values_vector=999")

```


#### Statistics of the new harmonized variable

```{r descript w5, echo=F}

vbl <- ALSA_ds_w5$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w5, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w5$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w5,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation


### Wave 6


#### Study-specific variable description

| **Name** | `CURRWKW6`|
|-|-|
| **Label** | `paid employment`|
| **Table name**  | `ALSA-Wave6` |
| **Categories** | `1 = yes`<br/>`2 = no` |
| **Missings** | `0 = no answer`<br/>`NA` |
| **Description** |  |

```{r assign w6, echo=F}

opal.assign.table.tibble(o, 'ALSA_w6','ALSA.ALSA-Wave6',variables=list('CURRWKW6'), missings = T)
ALSA_w6 <- opal.execute(o,'ALSA_w6')

```

```{r local w6, echo=F}

vbl <- ALSA_w6$CURRWKW6
kable(Categorical_summary(vbl, missing_values = c(0))[3], caption = names(ALSA_w6)[2])
kable(Categorical_summary(vbl, missing_values = c(0))[1], caption = "Category")
ggplot(ALSA_w6, aes(x=factor(CURRWKW6))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `retired previously != 1 and CURRWKW6 == 1 into 0`
* `retired previously != 1 and CURRWKW6 == 0 into 995`
* `retired previously == 1 into 1`
* `NA into 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w6}

ALSA_w6 <- left_join(ALSA_w6,ALSA_ds, by="id")

ALSA_ds_w6 <- tibble(id=ALSA_w6$id)
ALSA_ds_w6$retired <- rep(999,length(ALSA_w6$id))
ALSA_ds_w6$retired[which(ALSA_w6$retired==1)] <- 1
ALSA_ds_w6$retired[which((ALSA_w6$retired!=1 | is.na(ALSA_w6$retired)) & ALSA_w6$CURRWKW6==1)] <- 0
ALSA_ds_w6$retired[which((ALSA_w6$retired!=1 | is.na(ALSA_w6$retired)) & ALSA_w6$CURRWKW6==0)] <- 995
ALSA_ds_w6$age_retired <- car::recode(ALSA_w6$age_retired, "NA=999")

# Update of global table
assign_merge <- function(endtib,sttib,log_vec=c(T),col_end_index=NULL,col_st_index=NULL){
  newtib <- left_join(endtib,sttib,by="id")
  newtib[log_vec,col_end_index] <- newtib[log_vec,col_st_index]
  id_ind <- match("id",names(newtib))
  newtib <- newtib[,c(id_ind,col_end_index)]
  names(newtib)[2] <- "x"
  newtib$x <- car::recode(newtib$x,"NA=999")
  names(newtib)[2] <- names(endtib)[col_end_index]
  newtib
}
ALSA_ds_prov <- ALSA_ds[,c(1,2)]
ALSA_ds_prov <- assign_merge(ALSA_ds_prov,ALSA_ds_w6,log_vec=(ALSA_ds_prov$id %in% ALSA_ds_w6$id),col_end_index = 2, col_st_index = 3)
ALSA_ds$retired <- ALSA_ds_prov$retired
rm(ALSA_ds_prov)

```


#### Statistics of the new harmonized variable

```{r descript w6, echo=F}

vbl <- ALSA_ds_w6$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w6, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w6$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w6,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation

```{r crosstabulation w6, echo=F}

AfterH <- table(ALSA_ds_w6$retired)
X <- c(
  sum((ALSA_w6$retired!=1 | is.na(ALSA_w6$retired)) & ALSA_w6$CURRWKW6==1,na.rm = T),
  sum(ALSA_w6$retired==1,na.rm = T),
  sum((ALSA_w6$retired!=1 | is.na(ALSA_w6$retired)) & ALSA_w6$CURRWKW6==0,na.rm = T)
)
BeforeH <- c(X[1:3],length(ALSA_w6$id)-sum(X))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("CURRWKW6==1->0","retired->1","CURRWKW6==0->995","otherwise->999")
kable(C)

```



### Wave 7


#### Study-specific variable description

| **Name** | `currwkw7`|
|-|-|
| **Label** | `paid employment`|
| **Table name**  | `ALSA-Wave7` |
| **Categories** | `1 = Yes`<br/>`2 = No` |
| **Missings** | `0 = Not Answered`<br/>`NA` |
| **Description** |  |

```{r assign w7, echo=F}

opal.assign.table.tibble(o, 'ALSA_w7','ALSA.ALSA-Wave7',variables=list('currwkw7'), missings = T)
ALSA_w7 <- opal.execute(o,'ALSA_w7')

```

```{r local w7, echo=F}

vbl <- ALSA_w7$currwkw7
kable(Categorical_summary(vbl, missing_values = c(0))[3], caption = names(ALSA_w7)[2])
kable(Categorical_summary(vbl, missing_values = c(0))[1], caption = "Category")
ggplot(ALSA_w7, aes(x=factor(currwkw7))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `retired previously != 1 and currwkw7 == 1 into 0`
* `retired previously != 1 and currwkw7 == 0 into 995`
* `retired previously == 1 into 1`
* `NA into 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w7}

ALSA_w7 <- left_join(ALSA_w7,ALSA_ds, by="id")

ALSA_ds_w7 <- tibble(id=ALSA_w7$id)
ALSA_ds_w7$retired <- rep(999,length(ALSA_w7$id))
ALSA_ds_w7$retired[which(ALSA_w7$retired==1)] <- 1
ALSA_ds_w7$retired[which((ALSA_w7$retired!=1 | is.na(ALSA_w7$retired)) & ALSA_w7$currwkw7==1)] <- 0
ALSA_ds_w7$retired[which((ALSA_w7$retired!=1 | is.na(ALSA_w7$retired)) & ALSA_w7$currwkw7==0)] <- 995
ALSA_ds_w7$age_retired <- car::recode(ALSA_w7$age_retired, "NA=999")

# Update of global table
assign_merge <- function(endtib,sttib,log_vec=c(T),col_end_index=NULL,col_st_index=NULL){
  newtib <- left_join(endtib,sttib,by="id")
  newtib[log_vec,col_end_index] <- newtib[log_vec,col_st_index]
  id_ind <- match("id",names(newtib))
  newtib <- newtib[,c(id_ind,col_end_index)]
  names(newtib)[2] <- "x"
  newtib$x <- car::recode(newtib$x,"NA=999")
  names(newtib)[2] <- names(endtib)[col_end_index]
  newtib
}
ALSA_ds_prov <- ALSA_ds[,c(1,2)]
ALSA_ds_prov <- assign_merge(ALSA_ds_prov,ALSA_ds_w7,log_vec=(ALSA_ds_prov$id %in% ALSA_ds_w7$id),col_end_index = 2, col_st_index = 3)
ALSA_ds$retired <- ALSA_ds_prov$retired
rm(ALSA_ds_prov)

```


#### Statistics of the new harmonized variable

```{r descript w7, echo=F}

vbl <- ALSA_ds_w7$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w7, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w7$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w7,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation

```{r crosstabulation w7, echo=F}

AfterH <- table(ALSA_ds_w7$retired)
X <- c(
  sum((ALSA_w7$retired!=1 | is.na(ALSA_w7$retired)) & ALSA_w7$currwkw7==1,na.rm = T),
  sum(ALSA_w7$retired==1,na.rm = T)
)
BeforeH <- c(X[1:2],length(ALSA_w7$id)-sum(X))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("currwkw7==1->0","retired->1","otherwise->999")
kable(C)

```




### Wave 8

#### Study-specific variable description

There is no variable.

```{r assign w8, echo=F}

load(paste0(datafolder,"w8/ybirth.RData"))
ALSA_w8 <- tibble(id=ybirth$id)

```

#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `if retired == 1 previously, retired = 1`
* `otherwise, retired = 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w8}

ALSA_w8 <- left_join(ALSA_w8,ALSA_ds, by="id")
ALSA_ds_w8 <- tibble(id=ALSA_w8$id)
ALSA_ds_w8$retired <- car::recode(ALSA_w8$retired, "c(0,NA)=999; miss_values_vector=999")
ALSA_ds_w8$age_retired <- car::recode(ALSA_w8$age_retired, "NA=999; miss_values_vector=999")

```


#### Statistics of the new harmonized variable

```{r descript w8, echo=F}

vbl <- ALSA_ds_w8$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w8, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w8$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w8,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation




### Wave 9

#### Study-specific variable description

There is no variable.

```{r assign w9, echo=F}

load(paste0(datafolder,"w9/ybirth.RData"))
ALSA_w9 <- tibble(id=ybirth$id)

```

#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `if retired == 1 previously, retired = 1`
* `otherwise, retired = 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w9}

ALSA_w9 <- left_join(ALSA_w9,ALSA_ds, by="id")
ALSA_ds_w9 <- tibble(id=ALSA_w9$id)
ALSA_ds_w9$retired <- car::recode(ALSA_w9$retired, "c(0,NA)=999; miss_values_vector=999")
ALSA_ds_w9$age_retired <- car::recode(ALSA_w9$age_retired, "NA=999; miss_values_vector=999")

```


#### Statistics of the new harmonized variable

```{r descript w9, echo=F}

vbl <- ALSA_ds_w9$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w9, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w9$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w9,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation



### Wave 10

#### Study-specific variable description

There is no variable.

```{r assign w10, echo=F}

load(paste0(datafolder,"w10/ybirth.RData"))
ALSA_w10 <- tibble(id=ybirth$id)

```

#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `if retired == 1 previously, retired = 1`
* `otherwise, retired = 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w10}

ALSA_w10 <- left_join(ALSA_w10,ALSA_ds, by="id")
ALSA_ds_w10 <- tibble(id=ALSA_w10$id)
ALSA_ds_w10$retired <- car::recode(ALSA_w10$retired, "c(0,NA)=999; miss_values_vector=999")
ALSA_ds_w10$age_retired <- car::recode(ALSA_w10$age_retired, "NA=999; miss_values_vector=999")

```


#### Statistics of the new harmonized variable

```{r descript w10, echo=F}

vbl <- ALSA_ds_w10$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w10, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w10$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w10,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation



### Wave 11


#### Study-specific variable description

| **Name** | `CURRWKW11`|
|-|-|
| **Label** | `currently work in a paid job`|
| **Table name**  | `ALSA-Wave11` |
| **Categories** | `1 = Yes`<br/>`0 = No` |
| **Missings** | `NA` |
| **Description** |  |

```{r assign w11, echo=F}

opal.assign.table.tibble(o, 'ALSA_w11','ALSA.ALSA-Wave11',variables=list('CURRWKW11'), missings = T)
ALSA_w11 <- opal.execute(o,'ALSA_w11')

```

```{r local w11, echo=F}

vbl <- ALSA_w11$CURRWKW11
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(ALSA_w11)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(ALSA_w11, aes(x=factor(CURRWKW11))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `retired previously != 1 and CURRWKW11 == 1 into 0`
* `retired previously == 1 into 1`
* `NA into 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w11}

ALSA_w11 <- left_join(ALSA_w11,ALSA_ds, by="id")

ALSA_ds_w11 <- tibble(id=ALSA_w11$id)
ALSA_ds_w11$retired <- rep(999,length(ALSA_w11$id))
ALSA_ds_w11$retired[which(ALSA_w11$retired==1)] <- 1
ALSA_ds_w11$retired[which((ALSA_w11$retired!=1 | is.na(ALSA_w11$retired)) & ALSA_w11$CURRWKW11==1)] <- 0
ALSA_ds_w11$age_retired <- car::recode(ALSA_w11$age_retired, "NA=999")

# Update of global table
assign_merge <- function(endtib,sttib,log_vec=c(T),col_end_index=NULL,col_st_index=NULL){
  newtib <- left_join(endtib,sttib,by="id")
  newtib[log_vec,col_end_index] <- newtib[log_vec,col_st_index]
  id_ind <- match("id",names(newtib))
  newtib <- newtib[,c(id_ind,col_end_index)]
  names(newtib)[2] <- "x"
  newtib$x <- car::recode(newtib$x,"NA=999")
  names(newtib)[2] <- names(endtib)[col_end_index]
  newtib
}
ALSA_ds_prov <- ALSA_ds[,c(1,2)]
ALSA_ds_prov <- assign_merge(ALSA_ds_prov,ALSA_ds_w11,log_vec=(ALSA_ds_prov$id %in% ALSA_ds_w11$id),col_end_index = 2, col_st_index = 3)
ALSA_ds$retired <- ALSA_ds_prov$retired
rm(ALSA_ds_prov)

```


#### Statistics of the new harmonized variable

```{r descript w11, echo=F}

vbl <- ALSA_ds_w11$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w11, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w11$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w11,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation

```{r crosstabulation w11, echo=F}

AfterH <- table(ALSA_ds_w11$retired)
X <- c(
  sum((ALSA_w11$retired!=1 | is.na(ALSA_w11$retired)) & ALSA_w11$CURRWKW11==1,na.rm = T),
  sum(ALSA_w11$retired==1,na.rm = T)
)
BeforeH <- c(X[1:2],length(ALSA_w11$id)-sum(X))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("currwkw11==1->0","retired->1","otherwise->999")
kable(C)

```




### Wave 12


#### Study-specific variable description

| **Name** | `CURRWKW12`|
|-|-|
| **Label** | `currently work in a paid job`|
| **Table name**  | `ALSA-Wave12` |
| **Categories** | `1 = Yes`<br/>`0 = No` |
| **Missings** | `NA` |
| **Description** |  |

```{r assign w12, echo=F}

opal.assign.table.tibble(o, 'ALSA_w12','ALSA.ALSA-Wave12',variables=list('CURRWKW12'), missings = T)
ALSA_w12 <- opal.execute(o,'ALSA_w12')

```

```{r local w12, echo=F}

vbl <- ALSA_w12$CURRWKW12
kable(Categorical_summary(vbl, missing_values = NA)[2], caption = names(ALSA_w12)[2])
kable(Categorical_summary(vbl, missing_values = NA)[1], caption = "Category")
ggplot(ALSA_w12, aes(x=factor(CURRWKW12))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(label(vbl)) + ylab("Frequency")

```


#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `retired previously != 1 and CURRWKW12 == 1 into 0`
* `retired previously == 1 into 1`
* `NA into 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w12}

ALSA_w12 <- left_join(ALSA_w12,ALSA_ds, by="id")

ALSA_ds_w12 <- tibble(id=ALSA_w12$id)
ALSA_ds_w12$retired <- rep(999,length(ALSA_w12$id))
ALSA_ds_w12$retired[which(ALSA_w12$retired==1)] <- 1
ALSA_ds_w12$retired[which((ALSA_w12$retired!=1 | is.na(ALSA_w12$retired)) & ALSA_w12$CURRWKW12==1)] <- 0
ALSA_ds_w12$age_retired <- car::recode(ALSA_w12$age_retired, "NA=999")

# Update of global table
assign_merge <- function(endtib,sttib,log_vec=c(T),col_end_index=NULL,col_st_index=NULL){
  newtib <- left_join(endtib,sttib,by="id")
  newtib[log_vec,col_end_index] <- newtib[log_vec,col_st_index]
  id_ind <- match("id",names(newtib))
  newtib <- newtib[,c(id_ind,col_end_index)]
  names(newtib)[2] <- "x"
  newtib$x <- car::recode(newtib$x,"NA=999")
  names(newtib)[2] <- names(endtib)[col_end_index]
  newtib
}
ALSA_ds_prov <- ALSA_ds[,c(1,2)]
ALSA_ds_prov <- assign_merge(ALSA_ds_prov,ALSA_ds_w12,log_vec=(ALSA_ds_prov$id %in% ALSA_ds_w12$id),col_end_index = 2, col_st_index = 3)
ALSA_ds$retired <- ALSA_ds_prov$retired
rm(ALSA_ds_prov)

```


#### Statistics of the new harmonized variable

```{r descript w12, echo=F}

vbl <- ALSA_ds_w12$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w12, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w12$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w12,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation

```{r crosstabulation w12, echo=F}

AfterH <- table(ALSA_ds_w12$retired)
X <- c(
  sum((ALSA_w12$retired!=1 | is.na(ALSA_w12$retired)) & ALSA_w12$CURRWKW12==1,na.rm = T),
  sum(ALSA_w12$retired==1,na.rm = T)
)
BeforeH <- c(X[2],length(ALSA_w12$id)-sum(X))
C <- rbind(BeforeH,AfterH)
colnames(C) <- c("retired->1","otherwise->999")
kable(C)

```




### Wave 13

#### Study-specific variable description

There is no variable.

```{r assign w13, echo=F}

load(paste0(datafolder,"w13/ybirth.RData"))
ALSA_w13 <- tibble(id=ybirth$id)

```

#### Harmonisation algorithm

To compute `retired` from the study-specific variable it has to be recoded as follows:

* `if retired == 1 previously, retired = 1`
* `otherwise, retired = 999`

To compute `age_retired` from the study-specific variable it has to be recoded as follows:

* `age_retired = age_retired from w1`

**R script:**

```{r harmo w13}

ALSA_w13 <- left_join(ALSA_w13,ALSA_ds, by="id")
ALSA_ds_w13 <- tibble(id=ALSA_w13$id)
ALSA_ds_w13$retired <- car::recode(ALSA_w13$retired, "c(0,NA)=999; miss_values_vector=999")
ALSA_ds_w13$age_retired <- car::recode(ALSA_w13$age_retired, "NA=999; miss_values_vector=999")

```


#### Statistics of the new harmonized variable

```{r descript w13, echo=F}

vbl <- ALSA_ds_w13$retired
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[3], caption = ds_label)
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[1], caption = "Category")
kable(Categorical_summary(vbl, missing_values = miss_values_vector)[2], caption = "Type of missing")
ggplot(ALSA_ds_w13, aes(x=factor(retired))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label) + ylab("Frequency")

vbl <- ALSA_ds_w13$age_retired
kable(Continuous_summary(var = vbl, missing_values = miss_values_vector)[3], caption = ds_label_age)
kable(Continuous_summary(vbl, missing_values = miss_values_vector)[2], caption = "Values")
pander(Continuous_summary(vbl, missing_values = miss_values_vector)$summary, caption = "Summary")
ggplot(filter(ALSA_ds_w13,!vbl %in% miss_values_vector), aes(age_retired)) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab(ds_label_age) + ylab("Frequency")

```


#### Validation




## Summary of descriptive statistics of the harmonised variable accross populations and waves

Two tables are generated: 

1. Percentages of categories in each harmonised variable.

2. Frequencies and percentages of individuals with different trajectories. To avoid too large table, Only trajectories with percentages larger than 0.3 are shown.






```{r summ}

l.hds <- list(w1 = ALSA_ds_w1, w2 = ALSA_ds_w2, w3 = ALSA_ds_w3, w4 = ALSA_ds_w4, w5 = ALSA_ds_w5, w6 = ALSA_ds_w6, w7 = ALSA_ds_w7, w8 = ALSA_ds_w8, w9 = ALSA_ds_w9, w10 = ALSA_ds_w10, w11 = ALSA_ds_w11, w12 = ALSA_ds_w12, w13 = ALSA_ds_w13)

for(name in names(l.hds)) {
  aux_object <- l.hds[[name]]
  label(l.hds[[name]][[2]]) <- label(aux_object[[2]]) <- ds_label # Text value assigned at the DS description, at the top
  l.hds[[name]][[2]] <- labelled(l.hds[[name]][[2]], labels = cat_label)
  label(l.hds[[name]][[3]]) <- label(aux_object[[3]]) <- ds_label_age # Text value assigned at the DS description, at the top
  l.hds[[name]][[3]] <- labelled(l.hds[[name]][[3]], labels = cont_label)
  aux_object[[2]] <- car::recode(aux_object[[2]], "miss_values_vector=NA")
  aux_object[[2]] <- labelled(aux_object[[2]], labels = cat_label[1:2])
  m_name <- paste0("ALSA_m_ds_",name)
  assign(m_name, aux_object)
  rm(aux_object)
}

t.hds <- frq(l.hds[[1]][2])[[1]][,c(1,2)] 
for (i in seq_along(l.hds)){
  t.hds[2+i] <- frq(l.hds[[i]][2])[[1]][,4] 
}
t.hds[13,] <- c("n", "sample size", sapply(l.hds,function(wave) length(wave[[1]]))
             #length(l.hds[[1]][[2]]), length(l.hds[[2]][[2]]) , length(l.hds[[3]][[2]]), length(l.hds[[4]][[2]]), length(l.hds[[5]][[2]]), length(l.hds[[6]][[2]]), length(l.hds[[7]][[2]]) , length(l.hds[[8]][[2]]), length(l.hds[[9]][[2]]), length(l.hds[[10]][[2]]), length(l.hds[[11]][[2]]), length(l.hds[[12]][[2]]), length(l.hds[[13]][[2]])
             )
names(t.hds)<-c("val", "label", names(l.hds))
kable(t.hds)

dbb <- get(paste0("ALSA_m_ds_",names(l.hds)[1]))[,c("id","retired")]
for(ind in 2:length(l.hds)){
  dbb <- merge(dbb, get(paste0("ALSA_m_ds_",names(l.hds)[ind]))[,c("id","retired")], by = "id", suffixes = c("", paste0(".",names(l.hds)[ind])), all = T)
}
names(dbb) <- c("id", names(l.hds))

v.dbb <- dbb[,2]
for(ind in 2:length(l.hds)){
  v.dbb <- paste(v.dbb,dbb[,ind+1],sep="")
}

#v.dbb <- paste(dbb[,2],dbb[,3],dbb[,4],dbb[,5],dbb[,6],dbb[,7],dbb[,8],dbb[,9],dbb[,10],dbb[,11],dbb[,12],dbb[,13],dbb[,14],sep="")
f.dbb <- frq(v.dbb)[[1]][,c(1,2,4)]
kable(f.dbb[f.dbb[,3]>=0.3,])

```


<!-- ########################################################## --> 
<!-- # Save -->
```{r save, include=FALSE, echo=F}


for(index in seq_along(l.hds)){
  retired <- l.hds[[index]]
  save(retired, file = paste0(datafolder,names(l.hds)[index],"/retired.RData"))
}

```
<!-- ########################################################## --> 

# Quality estimation



<!-- ########################################################## -->
<!--- # Close OPAL R Session -->
```{r closeRsession, echo=FALSE}
opal.logout(o)
```





