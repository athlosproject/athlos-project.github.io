---
title: "Month of Birth"
output:
  html_document:
    highlight: tango
    number_sections: yes
    theme: united
    toc: yes
    toc_depth: 6
    toc_float:
      collapsed: yes
      smooth_scroll: yes
---
  
<!-- ########################################################## --> 

<!-- # Installation, loading packages -->
<!-- **R script:** -->

```{r setup, include=FALSE}
source("M:/WPs/WP1/Maelstrom/data_process/athlos-project.github.io/setup_iago.r")
```
<!-- ########################################################## --> 







# Description of DataSchema variable 

The description of harmonised variable is the following:


* Short name: `mbirth`
* Variable label: `month of birth`
* Variable description: `Month of birth`
* Domain: `Socio-demographic and economic characteristics`
* Value type: `continuous`
* Category missings: 

**Code** | **Category Label**
----| ------------------
991 | CAPI/interviewer error
992 | Impute
993 | Disable to measure
994 | Not attempt/not done
995 | Does not answer
996 | Not applicable
997 | Refuse
998 | Do not know
999 | Missing


# Data process


## Australia

### Wave 1 

#### Study-specific variable description



| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |
    
```{r assign1, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
```

```{r local1, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

vari <- ALSA_w1_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w1_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w1_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo1}
ALSA_ds_w1 <- tibble(id=ALSA_w1_ybirth$id)
ALSA_ds_w1$mbirth <- car::recode(ALSA_w1_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w1$mbirth <- labelled(ALSA_ds_w1$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript1, echo=F}
vari <- ALSA_ds_w1$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w1$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w1, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation1, echo=F}
BeforeH <- table(ALSA_w1_ybirth$Month)
AfterH <- table(ALSA_ds_w1$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL1, echo=F}

#opal.assign.data(o, 'ALSA_ds_w1', ALSA_ds_w1)
#opal.symbol_import(o,'ALSA_ds_w1', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w1 <- ALSA_ds_w1
ALSA_m_ds_w1$mbirth <- car::recode(ALSA_m_ds_w1$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w1', ALSA_m_ds_w1)
#opal.symbol_import(o,'ALSA_m_ds_w1', project='_Athlos_Harm_Dataset')
```





### Wave 2 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |


```{r assign2, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w2_id','ALSA.ALSA-Wave2',
                         variables=list('DATE'), missings = TRUE)
```

```{r local2, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w2_id <- opal.execute(o,'ALSA_w2_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w2_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w2_id)


vari <- ALSA_w2_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w2_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w2_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo2}
ALSA_ds_w2 <- tibble(id=ALSA_w2_ybirth$id)
ALSA_ds_w2$mbirth <- car::recode(ALSA_w2_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w2$mbirth <- labelled(ALSA_ds_w2$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript2, echo=F}
vari <- ALSA_ds_w2$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w2$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w2, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation2, echo=F}
BeforeH <- table(ALSA_w2_ybirth$Month)
AfterH <- table(ALSA_ds_w2$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```


```{r importOPAL2, echo=F}

#opal.assign.data(o, 'ALSA_ds_w2', ALSA_ds_w2)
#opal.symbol_import(o,'ALSA_ds_w2', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w2 <- ALSA_ds_w2
ALSA_m_ds_w2$mbirth <- car::recode(ALSA_m_ds_w2$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w2', ALSA_m_ds_w2)
#opal.symbol_import(o,'ALSA_m_ds_w2', project='_Athlos_Harm_Dataset')
```






### Wave 3 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |

    
```{r assign3, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w3_id','ALSA.ALSA-Wave3',
                         variables=list('SEXW3'), missings = TRUE)
```

```{r local3, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w3_id <- opal.execute(o,'ALSA_w3_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w3_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w3_id)


vari <- ALSA_w3_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w3_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w3_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo3}
ALSA_ds_w3 <- tibble(id=ALSA_w3_ybirth$id)
ALSA_ds_w3$mbirth <- car::recode(ALSA_w3_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w3$mbirth <- labelled(ALSA_ds_w3$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript3, echo=F}
vari <- ALSA_ds_w3$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w3$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w3, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation3, echo=F}
BeforeH <- table(ALSA_w3_ybirth$Month)
AfterH <- table(ALSA_ds_w3$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```




```{r importOPAL3, echo=F}

#opal.assign.data(o, 'ALSA_ds_w3', ALSA_ds_w3)
#opal.symbol_import(o,'ALSA_ds_w3', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w3 <- ALSA_ds_w3
ALSA_m_ds_w3$mbirth <- car::recode(ALSA_m_ds_w3$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w3', ALSA_m_ds_w3)
#opal.symbol_import(o,'ALSA_m_ds_w3', project='_Athlos_Harm_Dataset')
```






### Wave 4 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |
    
```{r assign4, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w4_id','ALSA.ALSA-Wave4',
                         variables=list('W4QUNO'), missings = TRUE)
```

```{r local4, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w4_id <- opal.execute(o,'ALSA_w4_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w4_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w4_id)

vari <- ALSA_w4_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w4_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w4_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo4}
ALSA_ds_w4 <- tibble(id=ALSA_w4_ybirth$id)
ALSA_ds_w4$mbirth <- car::recode(ALSA_w4_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w4$mbirth <- labelled(ALSA_ds_w4$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript4, echo=F}
vari <- ALSA_ds_w4$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w4$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w4, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation4, echo=F}
BeforeH <- table(ALSA_w4_ybirth$Month)
AfterH <- table(ALSA_ds_w4$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL4, echo=F}

#opal.assign.data(o, 'ALSA_ds_w4', ALSA_ds_w4)
#opal.symbol_import(o,'ALSA_ds_w4', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w4 <- ALSA_ds_w4
ALSA_m_ds_w4$mbirth <- car::recode(ALSA_m_ds_w4$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w4', ALSA_m_ds_w4)
#opal.symbol_import(o,'ALSA_m_ds_w4', project='_Athlos_Harm_Dataset')
```







### Wave 5 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |
    
```{r assign5, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w5_id','ALSA.ALSA-Wave5',
                         variables=list('INTTYPW5'), missings = TRUE)
```

```{r local5, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w5_id <- opal.execute(o,'ALSA_w5_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w5_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w5_id)

vari <- ALSA_w5_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w5_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w5_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo5}
ALSA_ds_w5 <- tibble(id=ALSA_w5_ybirth$id)
ALSA_ds_w5$mbirth <- car::recode(ALSA_w5_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w5$mbirth <- labelled(ALSA_ds_w5$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript5, echo=F}
vari <- ALSA_ds_w5$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w5$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w5, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation5, echo=F}
BeforeH <- table(ALSA_w5_ybirth$Month)
AfterH <- table(ALSA_ds_w5$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL5, echo=F}

#opal.assign.data(o, 'ALSA_ds_w5', ALSA_ds_w5)
#opal.symbol_import(o,'ALSA_ds_w5', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w5 <- ALSA_ds_w5
ALSA_m_ds_w5$mbirth <- car::recode(ALSA_m_ds_w5$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w5', ALSA_m_ds_w5)
#opal.symbol_import(o,'ALSA_m_ds_w5', project='_Athlos_Harm_Dataset')
```






### Wave 6 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |

    
```{r assign6, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w6_id','ALSA.ALSA-Wave6',
                         variables=list('AGEW6'), missings = TRUE)
```

```{r local6, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w6_id <- opal.execute(o,'ALSA_w6_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w6_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w6_id)

vari <- ALSA_w6_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w6_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w6_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`


**R script:**

```{r harmo6}
ALSA_ds_w6 <- tibble(id=ALSA_w6_ybirth$id)
ALSA_ds_w6$mbirth <- car::recode(ALSA_w6_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w6$mbirth <- labelled(ALSA_ds_w6$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript6, echo=F}
vari <- ALSA_ds_w6$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w6$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w6, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation6, echo=F}
BeforeH <- table(ALSA_w6_ybirth$Month)
AfterH <- table(ALSA_ds_w6$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL6, echo=F}

#opal.assign.data(o, 'ALSA_ds_w6', ALSA_ds_w6)
#opal.symbol_import(o,'ALSA_ds_w6', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w6 <- ALSA_ds_w6
ALSA_m_ds_w6$mbirth <- car::recode(ALSA_m_ds_w6$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w6', ALSA_m_ds_w6)
#opal.symbol_import(o,'ALSA_m_ds_w6', project='_Athlos_Harm_Dataset')
```







### Wave 7 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |

    
```{r assign7, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w7_id','ALSA.ALSA-Wave7',
                         variables=list('sex'), missings = TRUE)
```

```{r local7, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w7_id <- opal.execute(o,'ALSA_w7_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w7_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w7_id)

vari <- ALSA_w7_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w7_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w7_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo7}
ALSA_ds_w7 <- tibble(id=ALSA_w7_ybirth$id)
ALSA_ds_w7$mbirth <- car::recode(ALSA_w7_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w7$mbirth <- labelled(ALSA_ds_w7$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript7, echo=F}
vari <- ALSA_ds_w7$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w7$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w7, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation7, echo=F}
BeforeH <- table(ALSA_w7_ybirth$Month)
AfterH <- table(ALSA_ds_w7$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL7, echo=F}

#opal.assign.data(o, 'ALSA_ds_w7', ALSA_ds_w7)
#opal.symbol_import(o,'ALSA_ds_w7', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w7 <- ALSA_ds_w7
ALSA_m_ds_w7$mbirth <- car::recode(ALSA_m_ds_w7$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w7', ALSA_m_ds_w7)
#opal.symbol_import(o,'ALSA_m_ds_w7', project='_Athlos_Harm_Dataset')
```






### Wave 8 

#### Study-specific variable description


| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |

    
```{r assign8, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w8_id','ALSA.ALSA-Wave8',
                         variables=list('Date'), missings = TRUE)
```

```{r local8, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w8_id <- opal.execute(o,'ALSA_w8_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w8_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w8_id)

vari <- ALSA_w8_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w8_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w8_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo8}
ALSA_ds_w8 <- tibble(id=ALSA_w8_ybirth$id)
ALSA_ds_w8$mbirth <- car::recode(ALSA_w8_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w8$mbirth <- labelled(ALSA_ds_w8$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript8, echo=F}
vari <- ALSA_ds_w8$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w8$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w8, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation8, echo=F}
BeforeH <- table(ALSA_w8_ybirth$Month)
AfterH <- table(ALSA_ds_w8$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL8, echo=F}

#opal.assign.data(o, 'ALSA_ds_w8', ALSA_ds_w8)
#opal.symbol_import(o,'ALSA_ds_w8', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w8 <- ALSA_ds_w8
ALSA_m_ds_w8$mbirth <- car::recode(ALSA_m_ds_w8$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w8', ALSA_m_ds_w8)
#opal.symbol_import(o,'ALSA_m_ds_w8', project='_Athlos_Harm_Dataset')
```






### Wave 9 

#### Study-specific variable description


| **Name** | `BIRTHDAT` |`dob`|
|----------|------------|-|
| **Label** | `Birthdate` |`Date of Birth`|
| **Table name** | `ALSA-Wave1` |`ALSA-Wave9`|
| **Missings** | `99999999 = Missing` |`NA; 2024-06-19; 1992-06-14`|
| **Description** |  |  |

    
```{r assign9, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w9_id','ALSA.ALSA-Wave9',
                         variables=list('sex'), missings = TRUE)
```

```{r local9, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w9_id <- opal.execute(o,'ALSA_w9_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w9_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w9_id)

vari <- ALSA_w9_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w9_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w9_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo9}
ALSA_ds_w9 <- tibble(id=ALSA_w9_ybirth$id)
ALSA_ds_w9$mbirth <- car::recode(ALSA_w9_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w9$mbirth <- labelled(ALSA_ds_w9$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript9, echo=F}
vari <- ALSA_ds_w9$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w9$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w9, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation9, echo=F}
BeforeH <- c(table(ALSA_w9_ybirth$Month), sum(is.na(ALSA_w9_ybirth$Month)))
AfterH <- table(ALSA_ds_w9$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- c(names(AfterH)[1:12], "NA->999")
kable(C)
```

We contrast with `dob`, the specific date of birth variable from wave 9:


```{r crosstabulation9_2, echo=F, results='asis'}
opal.assign.table.tibble(o, 'ALSA_w9_compr_mbirth','ALSA.ALSA-Wave9',
                         variables=list('dob'), missings = TRUE)

ALSA_w9_compr_mbirth <- opal.execute(o,'ALSA_w9_compr_mbirth')
ALSA_w9_compr_mbirth$mbirth_w9 <- month(ALSA_w9_compr_mbirth$dob) 


vari <- ALSA_w9_compr_mbirth$mbirth_w9
kable(Continuous_summary(vari, missing_values = NA)[3], caption = 'dob')
pander(Continuous_summary(vari, missing_values = NA)$summary, caption = "Summary")
pander(Continuous_summary(vari, missing_values = NA)$values_table, caption = "Values table")

ALSA_w9_compr_mbirth$mdiff <- ALSA_ds_w9$mbirth - ALSA_w9_compr_mbirth$mbirth_w9
ALSA_w9_compr_mbirth <- cbind(ALSA_w9_compr_mbirth,ALSA_ds_w9)
names(ALSA_w9_compr_mbirth)[6] <- "mbirth_w1"


mis1b9 <- sum(is.na(ALSA_w9_compr_mbirth[6,])&!is.na(ALSA_w9_compr_mbirth[3,]))
cat("Number of missings of month of birth in wave 1 but not missing in wave 9:",mis1b9)


Diffw1w9 <- as.data.frame(table(ALSA_w9_compr_mbirth$mdiff))
names(Diffw1w9) <- c("months of difference", "quantity of individuals")
kable(Diffw1w9,caption="Difference of months between waves 1 and 9 in the answers",align = 'c')

kable(ALSA_w9_compr_mbirth[which(ALSA_w9_compr_mbirth$mdiff %in% c(-10,1,3,5,6,9,10)),],align = 'c',caption="Specific individuals with non-trivial difference" )
```

Even finding 8 individuals with differences between wave 1 and wave 9, no changes are done.

```{r importOPAL9, echo=F}

#opal.assign.data(o, 'ALSA_ds_w9', ALSA_ds_w9)
#opal.symbol_import(o,'ALSA_ds_w9', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w9 <- ALSA_ds_w9
ALSA_m_ds_w9$mbirth <- car::recode(ALSA_m_ds_w9$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w9', ALSA_m_ds_w9)
#opal.symbol_import(o,'ALSA_m_ds_w9', project='_Athlos_Harm_Dataset')
```






### Wave 10 

#### Study-specific variable description


| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |

    
```{r assign10, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w10_id','ALSA.ALSA-Wave10',
                         variables=list('Date'), missings = TRUE)
```

```{r local10, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w10_id <- opal.execute(o,'ALSA_w10_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w10_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w10_id)

vari <- ALSA_w10_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w10_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w10_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo10}
ALSA_ds_w10 <- tibble(id=ALSA_w10_ybirth$id)
ALSA_ds_w10$mbirth <- car::recode(ALSA_w10_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w10$mbirth <- labelled(ALSA_ds_w10$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript10, echo=F}
vari <- ALSA_ds_w10$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w10$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w10, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation10, echo=F}
BeforeH <- table(ALSA_w10_ybirth$Month)
AfterH <- table(ALSA_ds_w10$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL10, echo=F}

#opal.assign.data(o, 'ALSA_ds_w10', ALSA_ds_w10)
#opal.symbol_import(o,'ALSA_ds_w10', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w10 <- ALSA_ds_w10
ALSA_m_ds_w10$mbirth <- car::recode(ALSA_m_ds_w10$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w10', ALSA_m_ds_w10)
#opal.symbol_import(o,'ALSA_m_ds_w10', project='_Athlos_Harm_Dataset')
```





### Wave 11 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |`dob`|
|----------|------------|-|
| **Label** | `Birthdate` |`date of birth`|
| **Table name** | `ALSA-Wave1` | `ALSA-Wave11` |
| **Missings** | `99999999 = Missing` |  |
| **Description** |  |  |


    
```{r assign11, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w11_id','ALSA.ALSA-Wave11',
                         variables=list('SEXW11'), missings = TRUE)
```

```{r local11, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w11_id <- opal.execute(o,'ALSA_w11_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w11_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w11_id)

vari <- ALSA_w11_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w11_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w11_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo11}
ALSA_ds_w11 <- tibble(id=ALSA_w11_ybirth$id)
ALSA_ds_w11$mbirth <- car::recode(ALSA_w11_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w11$mbirth <- labelled(ALSA_ds_w11$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript11, echo=F}
vari <- ALSA_ds_w11$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w11$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w11, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation11, echo=F}
BeforeH <- table(ALSA_w11_ybirth$Month)
AfterH <- table(ALSA_ds_w11$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```

We contrast with `dob`, the specific date of birth variable from wave 11:


```{r crosstabulation11_2, echo=F, results='asis'}
opal.assign.table.tibble(o, 'ALSA_w11_compr_mbirth','ALSA.ALSA-Wave11',
                         variables=list('dob'), missings = TRUE)

ALSA_w11_compr_mbirth <- opal.execute(o,'ALSA_w11_compr_mbirth')
#ALSA_w11_compr_mbirth <- cbind(ALSA_w11_compr, ALSA_w11_compr_mbirth$mbirth)
ALSA_w11_compr_mbirth$mbirth_w11 <- as.numeric(format(ALSA_w11_compr_mbirth$dob, '%m'))

vari <- ALSA_w11_compr_mbirth$mbirth_w11
kable(Continuous_summary(vari, missing_values = NA)[3], caption = 'dob')
pander(Continuous_summary(vari, missing_values = NA)$summary, caption = "Summary")
pander(Continuous_summary(vari, missing_values = NA)$values_table, caption = "Values table")

ALSA_w11_compr_mbirth$mbirth_w1 <- ALSA_ds_w11$mbirth
ALSA_w11_compr_mbirth$mdiff <- ALSA_w11_compr_mbirth$mbirth_w11 - ALSA_w11_compr_mbirth$mbirth_w1

mis1b11 <- sum(is.na(ALSA_w11_compr_mbirth$mbirth_1)&!is.na(ALSA_w11_compr_mbirth$mbirth_11))
cat("Number of missings of month of birth in wave 1 but not missing in wave 11:",mis1b11)

Diffw1w11 <- as.data.frame(table(ALSA_w11_compr_mbirth$mdiff))
names(Diffw1w11) <- c("months of difference", "quantity of individuals")
kable(Diffw1w11,caption="Difference of months between waves 1 and 11 in the answers",align = 'c')

kable(ALSA_w11_compr_mbirth[which(ALSA_w11_compr_mbirth$mdiff %in% c(-1,-5)),],align = 'c',caption="Specific individuals with non-trivial difference" )

```

Even finding 2 individuals with differences between wave 1 and wave 11, no changes are done.


```{r importOPAL11, echo=F}

#opal.assign.data(o, 'ALSA_ds_w11', ALSA_ds_w11)
#opal.symbol_import(o,'ALSA_ds_w11', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w11 <- ALSA_ds_w11
ALSA_m_ds_w11$mbirth <- car::recode(ALSA_m_ds_w11$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w11', ALSA_m_ds_w11)
#opal.symbol_import(o,'ALSA_m_ds_w11', project='_Athlos_Harm_Dataset')
```






### Wave 12 

#### Study-specific variable description

| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |

    
```{r assign12, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w12_id','ALSA.ALSA-Wave12',
                         variables=list('SEXW12'), missings = TRUE)
```

```{r local12, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w12_id <- opal.execute(o,'ALSA_w12_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w12_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w12_id)

vari <- ALSA_w12_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w12_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w12_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo12}
ALSA_ds_w12 <- tibble(id=ALSA_w12_ybirth$id)
ALSA_ds_w12$mbirth <- car::recode(ALSA_w12_ybirth$Month, "c(NA, 99)='999'")
ALSA_ds_w12$mbirth <- labelled(ALSA_ds_w12$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript12, echo=F}
vari <- ALSA_ds_w12$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w12$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w12, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation12, echo=F}
BeforeH <- table(ALSA_w12_ybirth$Month)
AfterH <- table(ALSA_ds_w12$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL12, echo=F}

#opal.assign.data(o, 'ALSA_ds_w12', ALSA_ds_w12)
#opal.symbol_import(o,'ALSA_ds_w12', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w12 <- ALSA_ds_w12
ALSA_m_ds_w12$mbirth <- car::recode(ALSA_m_ds_w12$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w12', ALSA_m_ds_w12)
#opal.symbol_import(o,'ALSA_m_ds_w12', project='_Athlos_Harm_Dataset')
```






### Wave 13 

#### Study-specific variable description


| **Name** | `BIRTHDAT` |
|----------|------------|
| **Label** | `Birthdate` |
| **Table name** | `ALSA-Wave1` |
| **Missings** | `99999999 = Missing` |
| **Description** |  |

    
```{r assign13, echo=F}
opal.assign.table.tibble(o, 'ALSA_w1_ybirth','ALSA.ALSA-Wave1',
                         variables=list('BIRTHDAT'), missings = TRUE)
opal.assign.table.tibble(o, 'ALSA_w13_id','ALSA.ALSA-Wave13',
                         variables=list('proxy'), missings = TRUE)
```

```{r local13, echo=F}
ALSA_w1_ybirth <- opal.execute(o,'ALSA_w1_ybirth')
ALSA_w13_id <- opal.execute(o,'ALSA_w13_id')
#sum(ALSA_w1_ybirth$BIRTHDAT==99999999)

#This function return a table with the date of birth separated by columns:
Date_of_birth <- function(date){
  date <- as.numeric(date)
  n <- length(date)
  D <- matrix(NA, nrow = n, ncol = 3)
  for(i in 1:n){
    if(log10(date[i])+1>=8){
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 3, 5), 
                                                                     last=c(2, 4, 8)))))
    }
    else{
      D[i,] <- as.numeric(t(sapply(date[i], 
                                               function(x) substring(x, first=c(1, 2, 4), 
                                                                     last=c(1, 3, 7)))))
    }
  }
  colnames(D) <- c("Day", "Month", "Year")
  D
}

ALSA_w1_ybirth <- cbind(ALSA_w1_ybirth, Date_of_birth(ALSA_w1_ybirth$BIRTHDAT))

Add_indiv <- function(old, new){
  
  new_2 <- matrix(NA, nrow = dim(new)[1], ncol=dim(old)[2])
  oldm <- as.matrix(old)
  
  for(i in 1:length(new$id)){
    if(sum(new$id[i]==old$id)>=1){
      new_2[i,] <- oldm[which(new$id[i]==old$id),]
    } else {
      new_2[i,] <- c(new$id[i], rep(NA,4))
    }
  }
  
  new_22 <- data.frame(id=new_2[,1], BIRTHDAT=as.numeric(new_2[,2]), Day=as.numeric(new_2[,3]), Month=as.numeric(new_2[,4]), Year=as.numeric(new_2[,5]))
  new_22
}


ALSA_w13_ybirth <- Add_indiv(old = ALSA_w1_ybirth, new = ALSA_w13_id)

vari <- ALSA_w13_ybirth$Month
kable(Categorical_summary(vari, missing_values = NA)[2], caption = 'BIRTHDAT')
kable(Categorical_summary(vari, missing_values = NA)[1], caption = "Category")
#Categorical_summary(var = ALSA_w13_ybirth$Month, missing_values = c(99))
ggplot(ALSA_w13_ybirth, aes(x=factor(Month))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Harmonisation algorithm
The harmonized variable is the same than the study-specific variable, but the missing values have to be recoded as follows:

* `NA into 999`
* `99 into 999`

**R script:**

```{r harmo13}
ALSA_ds_w13 <- tibble(id=ALSA_w13_ybirth$id)
ALSA_ds_w13$mbirth <- car::recode(ALSA_w13_ybirth$Month, "c(NA, 99)=999")
ALSA_ds_w13$mbirth <- labelled(ALSA_ds_w13$mbirth, labels = c("january"=1, "february"=2, "march"=3, "april"=4, "may"=5, "june"=6, "july"=7, "august"=8, "september"=9, "october"=10, "november"=11, "december"=12, "Missing"=999, "Do not know"=998, "Refuse"=997, "Not applicable"=996,  "Does not answer"=995,  "Not attempt not done"=994, "Disable to measure"=993, "Impute"=992, "CAPI interviewer error"=991))
```

#### Statistics of the new harmonised variable
```{r descript13, echo=F}
vari <- ALSA_ds_w13$mbirth
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[3], caption = "mbirth") # EL CAPTION ES EL NOMBRE DE LA HASMONISED VARIABLE
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[1], caption = "Category")
kable(Categorical_summary(vari, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))[2], caption = "Type of missing")
#Categorical_summary(var = ALSA_ds_w13$mbirth, missing_values = c(991, 992, 993, 994, 995, 996, 997, 998, 999))
ggplot(ALSA_ds_w13, aes(x=factor(mbirth))) + geom_bar(stat="count", width=0.4, fill="steelblue") + xlab("Month of birth") + ylab("Frequency")
```

#### Validation
```{r crosstabulation13, echo=F}
BeforeH <- table(ALSA_w13_ybirth$Month)
AfterH <- table(ALSA_ds_w13$mbirth)
C <- rbind(BeforeH,AfterH)
colnames(C) <- names(AfterH)
kable(C)
```



```{r importOPAL13, echo=F}

#opal.assign.data(o, 'ALSA_ds_w13', ALSA_ds_w13)
#opal.symbol_import(o,'ALSA_ds_w13', project='_Athlos_Harm_Dataset')

ALSA_m_ds_w13 <- ALSA_ds_w13
ALSA_m_ds_w13$mbirth <- car::recode(ALSA_m_ds_w13$mbirth, "c(991, 992, 993, 994, 995, 996, 997,998, 999)=NA")
#opal.assign.data(o, 'ALSA_m_ds_w13', ALSA_m_ds_w13)
#opal.symbol_import(o,'ALSA_m_ds_w13', project='_Athlos_Harm_Dataset')
```


# Quality estimation

Year of birth has to be computed by the formula: "date_birth"= "date" - "age".

An interval of error is expected since we do not know the exact age (month).
"date_birth" is computed as the difference between the actual age and the baseline interview date: 
baseline interview date-age in years. 



```{r save, echo=FALSE}
mbirth <- ALSA_ds_w1
save(mbirth, file = "../RData/w1/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w2
save(mbirth, file = "../RData/w2/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w3
save(mbirth, file = "../RData/w3/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w4
save(mbirth, file = "../RData/w4/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w5
save(mbirth, file = "../RData/w5/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w6
save(mbirth, file = "../RData/w6/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w7
save(mbirth, file = "../RData/w7/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w8
save(mbirth, file = "../RData/w8/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w9
save(mbirth, file = "../RData/w9/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w10
save(mbirth, file = "../RData/w10/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w11
save(mbirth, file = "../RData/w11/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w12
save(mbirth, file = "../RData/w12/mbirth.RData")
rm(mbirth)

mbirth <- ALSA_ds_w13
save(mbirth, file = "../RData/w13/mbirth.RData")
rm(mbirth)
```


```{r closeRsession, echo=FALSE}
opal.logout(o)
```

